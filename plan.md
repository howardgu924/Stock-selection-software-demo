# 方案：恒温器事件驱动回测系统

## 1. 目标

将当前“简化等权净值回测”升级为“事件驱动回测系统”，但不重写恒温器实盘/选股逻辑，不破坏现有前端、账户、自选组合、数据源和输出表读取方式。

本方案的核心是新增一条正式回测链路：

- 先校验并准备本地缓存数据。
- 再按交易日和固定事件点推进回测。
- 信号生成、订单执行、成交失败、持仓估值和报告输出分层处理。
- 旧简化回测可以保留，但必须明确标记为 `simplified_backtest`，不能作为正式回测默认结果。

## 2. 修改哪些模块

### 2.1 数据缓存层

涉及模块：

- `stock_picker/data/storage.py`
- `stock_picker/data/service.py`
- 可新增轻量模块：`stock_picker/data/limits.py`

调整方向：

- 保留现有日线缓存表和 `get_history()` 行为，避免影响现有策略、账户估值和页面功能。
- 新增或扩展专用于回测的事件价格缓存，覆盖 `symbol`、`date`、`time_point`、`frequency`、`adjust`、`source` 维度。
- 回测缓存必须保存 `prev_close`、涨停价、跌停价、停牌状态，以及 `morning_open`、`afternoon_open`、`close` 三个可执行时间点的涨跌停状态。
- 如果数据源不能直接提供涨跌停状态，使用 `prev_close`、板块规则和 ST 规则估算涨跌停价，并记录数据质量说明。
- 如果无法判断是否涨跌停或停牌，标记为 `limit_status_unknown`，不能静默当作可成交。
- 为正式回测提供缓存检查能力：缺数据时返回明确缺口和修复提示，而不是在逐日回测循环里临时联网抓取。

### 2.2 恒温器回测入口

涉及模块：

- `stock_picker/strategies/thermostat.py`
- 可新增轻量模块：`stock_picker/strategies/thermostat_backtest.py`

调整方向：

- 保留现有恒温器选股/建议逻辑的公开兼容入口。
- 将正式回测入口改为事件驱动回测。
- 将旧的简化等权净值逻辑保留为 legacy 或 simplified 路径，并在输出中明确标记，避免用户误以为它是正式回测。
- 正式回测继续复用现有恒温器信号判断能力，不在回测层重新发明一套选股策略。

### 2.3 事件驱动回测引擎

建议新增模块：

- `stock_picker/strategies/event_backtest.py`

职责：

- 管理交易日循环和事件点顺序。
- 每个交易日按固定顺序处理：
  - `morning_open`：只允许卖出、止损、风控，不允许新买。
  - `noon`：只做信号评估和订单计划，不成交。
  - `afternoon_open`：执行中午生成的买入/卖出计划。
  - `close`：更新市值、记录收盘状态、执行允许的卖出和最终清仓。
- 执行订单时检查停牌、涨跌停、未知涨跌停状态、现金、持仓、T+1 等约束。
- 所有失败订单必须记录到交易明细，不允许静默跳过。
- 最后一个交易日尝试收盘清仓，无法成交时记录未成交原因和最终持仓。

### 2.4 参数解析

涉及模块：

- `stock_picker/user/portfolio.py`
- `stock_picker/strategies/event_backtest.py`
- 可新增轻量模块：`stock_picker/strategies/backtest_params.py`

调整方向：

- 回测本金、佣金率、最低佣金、印花税率优先从当前账户设置读取。
- 如果用户在回测页面显式覆盖参数，报告中记录覆盖来源。
- 如果账户中缺少字段，使用默认值并在 Parameters 或 Data Quality 中记录来源。
- 不要求立即改变账户文件结构；除非确实需要新增字段，否则保持现有账户兼容。

### 2.5 报告输出

建议新增模块：

- `stock_picker/reporting/backtest_report.py`

职责：

- 生成正式回测的结构化结果。
- 输出 Web 简表所需数据。
- 输出 Excel 详细报告，至少包含：
  - Summary
  - Daily Portfolio
  - Daily Evaluation Detail
  - Trades
  - Positions
  - Symbol Performance
  - Data Quality
  - Parameters
- 所有展示字段使用中文或已有统一翻译，不出现“未翻译字段”或乱码。

### 2.6 Web 页面

涉及模块：

- `examples/web_app.py`
- `tests/test_web_app.py`

调整方向：

- 回测诊断页默认走正式事件驱动回测。
- 在运行正式回测前展示缓存状态、缺失数据、数据质量风险。
- 回测结果页展示摘要卡片、曲线或表格、交易明细摘要、数据质量提示和 Excel 下载入口。
- 如果保留旧简化回测入口，必须明确标注“简化回测”，并且不能作为默认正式结果。

## 3. 哪些行为保持不变

- 恒温器策略的正常选股运行入口保持不变。
- 现有账户、自选组合、股票池来源、市场范围、龙虎榜来源等功能保持不变。
- 现有数据源路由和日线缓存读取方式保持兼容。
- 现有前端顶部结构保持不变：恒温器策略 / 回测诊断 / 账户。
- `REQUIRED_ADVICE_COLUMNS` 和现有策略建议表结构保持兼容。
- 旧简化回测可以作为辅助诊断保留，但不再代表正式回测。
- 非回测相关测试和用户已验证的页面行为不应因为本次改动发生变化。

## 4. 测试策略

### 4.1 数据缓存测试

- 验证旧日线缓存仍可读写。
- 验证事件价格缓存能按 `symbol + date + time_point + frequency + adjust + source` 唯一读写。
- 验证缺少 `prev_close`、涨跌停价、停牌状态时会产生数据质量记录。
- 验证无法判断涨跌停状态时标记为 `limit_status_unknown`，不会默认可成交。
- 验证缓存缺失时正式回测拒绝进入逐日循环，并返回明确缺口。

### 4.2 涨跌停和停牌测试

- 普通股票、创业板/科创板、ST 股票分别测试涨跌停价估算。
- 测试 `limit_up` 时买入失败。
- 测试 `limit_down` 时卖出失败。
- 测试 `suspended` 时买卖失败。
- 测试 `limit_status_unknown` 时买卖失败并记录原因。

### 4.3 事件顺序测试

- 验证 `morning_open` 不允许新买，只允许卖出类动作。
- 验证 `noon` 只生成信号，不改变现金和持仓。
- 验证买入只能在 `afternoon_open` 成交。
- 验证收盘只做估值、允许卖出和最终清仓，不生成新的隔夜买入。
- 验证最终交易日会尝试清仓，失败时记录未成交原因。

### 4.4 账户和成交测试

- 验证佣金、最低佣金、印花税影响现金和收益。
- 验证现金不足时订单失败并记录。
- 验证仓位不足时卖出失败并记录。
- 验证 T+1 规则下当日买入股票不能当日卖出。
- 验证持仓成本、现金、总资产、已实现盈亏和浮动盈亏能连续计算。

### 4.5 报告测试

- 验证 Excel 包含所有指定 sheet。
- 验证关键列存在且名称为中文或统一翻译。
- 验证 Data Quality 能展示缺失字段、估算字段、未知涨跌停状态。
- 验证 Parameters 能展示账户参数、覆盖参数和默认参数来源。
- 验证 Web 结果页不出现“未翻译字段”或乱码。

### 4.6 回归测试

- 保留并运行现有恒温器策略测试。
- 保留并运行现有账户、自选组合、数据服务、Web 页面测试。
- 新增事件驱动回测测试后运行完整 `pytest`。

## 5. 风险点

- 数据源可能无法提供完整涨跌停和停牌状态，需要清晰区分“直接数据”“估算数据”和“未知数据”。
- 现有 SQLite 缓存已有用户数据，schema 迁移必须向后兼容。
- 事件驱动回测比简化净值回测更复杂，容易把策略信号、订单执行和报告生成混在一起。
- 大股票池回测可能耗时较长，必须避免逐日循环中重复联网抓数据。
- Excel 报告依赖可能在部分环境缺失，需要测试依赖和失败提示。
- Web 端既要新增正式回测展示，又不能影响现有恒温器策略页面。

## 6. 为什么这个方案不过度复杂

- 不重写恒温器策略，只把回测执行方式从简化净值改为事件驱动。
- 不引入分钟级全量撮合，只使用固定事件点：早盘开盘、中午评估、午后开盘、收盘。
- 不强制改造所有数据源，只在缓存层统一补齐、估算或标记未知。
- 不把旧回测立即删除，只降级为明确标记的简化辅助路径，降低回归风险。
- 报告生成独立出来，避免把 Excel、Web 展示和回测核心逻辑写在一起。
- 先用确定性假数据覆盖核心成交规则，再接入真实数据缓存，便于定位问题。

## 7. 分阶段实施顺序

### 阶段一：缓存和数据质量

先建立事件价格缓存、涨跌停状态字段、数据质量记录和缓存缺口检查。只有缓存规则可靠，后续回测才不会在执行层做错误假设。

### 阶段二：事件驱动引擎

实现交易日和事件点推进、订单约束、成交失败记录、现金持仓更新、最终清仓。该阶段使用假数据测试，不依赖真实网络数据。

### 阶段三：接入恒温器信号

把现有恒温器信号接入事件引擎，保持策略判断兼容，只改变回测中的执行机制。

### 阶段四：报告和 Web 展示

生成 Web 简表和 Excel 详细报告，加入数据质量提示和下载入口。

### 阶段五：回归验证

运行新增测试、现有 Web 测试、现有恒温器策略测试和完整测试集，确认正式回测上线没有破坏现有功能。
