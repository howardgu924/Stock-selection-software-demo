# Web 工作台进度与表单状态统一 plan

## 目标

基于当前 `spec.md`，本次计划聚焦 Web 工作台体验一致性：

- 恒温器所有股票池来源统一显示同一套 job/progress 进度区域。
- 账户页和恒温器页的表单状态按用途隔离。
- 操作类输入在提交成功后清空，配置类字段保留。
- 清理来源选项、账户路径、提示文案等现有不统一点。

本次不改恒温器策略计算逻辑，不改账户数据结构，不改行情数据源，不重写 Web 框架。

## 修改模块

### 1. `examples/web_app.py`

这是本次主要修改点。

计划修改内容：

- 统一恒温器运行入口，让手动输入、自选股组合、市场范围、龙虎榜都走同一类后台 job/progress 流程。
- 保留现有 `handle_thermostat()` 作为实际策略运行入口，避免改动策略计算。
- 调整 `render_thermostat_section()` 中的表单提交目标，使所有来源使用统一的运行体验。
- 保留或复用现有 `ThermostatJob`、`start_thermostat_job()`、`job_status_payload()`、`render_job_progress()` 能力。
- 梳理 `LAST_FORM` 使用方式，避免把一次表单提交的所有字段无差别带入其他页面或其他表单。
- 增加表单状态清理规则：操作类字段提交成功后清空，配置类字段保留。
- 统一账户路径字段的用户可见行为，避免 `path` 和 `account_path` 对用户造成两个账户路径的错觉。
- 检查股票池来源选项，确保下拉选项和实际可运行能力一致。
- 优化“现金不足”相关提示文案，区分账户总现金不足和建议仓位金额不足以买入一手。
- 保证新增或调整后的用户可见字段不出现“未翻译字段”。

### 2. `tests/test_web_app.py`

这是本次主要测试文件。

计划新增或调整测试：

- 所有恒温器股票池来源都渲染为统一 job/progress 表单。
- 普通恒温器 job 完成后能返回结果 HTML。
- job 失败时能返回错误状态和错误文案。
- 账户页各操作表单提交后只保留配置字段，清空临时字段。
- 自选组合相关操作提交后清空对应输入框。
- 删除组合后不再选中已删除组合。
- 账户路径在账户页和恒温器页之间保持一致语义。
- 回测页输入不会污染恒温器页配置。
- 股票池来源下拉不展示不可运行或误导性来源。
- 试探仓金额不足以买入一手的文案不再误导为账户总现金不足。
- Web 正常结果中不出现“未翻译字段”。

### 3. `tests/test_thermostat_strategy.py`

原则上不修改。

只有在“试探仓金额不足以买入一手”的文案来自策略结果而非 Web 翻译层时，才补一条小范围回归测试，验证 `reason` 或 `risk_note` 文案更准确。

### 4. `stock_picker/strategies/thermostat.py`

原则上不修改策略计算。

允许的唯一小改动范围：

- 如果当前“现金不足以买入一手”文案由策略层生成，且无法只在 Web 层准确区分，则只调整文案，不调整市场判断、个股判断、仓位比例、网格逻辑或输出字段结构。

### 5. 文档文件

可能更新：

- `spec.md`：仅当计划审查发现规格含糊或冲突时修正。
- `plan.md`：当前文件。
- `tasks.md`：后续 tasks 阶段生成，不在本计划阶段修改。

## 保持不变的行为

以下行为必须保持不变：

1. 恒温器策略核心计算结果不因 Web 进度流程统一而改变。
2. `handle_thermostat()` 的输入语义和输出表结构保持兼容。
3. 账户现金、持仓、交易流水的数据含义不变。
4. 自选组合仍然不是持仓，增删自选股不影响账户现金、持仓或交易流水。
5. 回测诊断页的核心计算结果不变。
6. 页面顶部仍是：恒温器策略 / 回测诊断 / 账户。
7. 数据源选择仍按当前方式工作。
8. 龙虎榜候选池生成逻辑不因进度统一而改变。
9. 现有 `REQUIRED_ADVICE_COLUMNS` 不因本次 Web 改动而改变。
10. 不新增真实交易或自动下单能力。

## 实施路径

### 阶段 1：锁定当前问题的测试

先写 Web 层回归测试，覆盖当前已知问题：

1. 手动输入来源应使用 job/progress 表单。
2. 自选股组合来源应使用 job/progress 表单。
3. 市场范围来源应使用 job/progress 表单。
4. 龙虎榜来源继续使用 job/progress 表单。
5. 普通恒温器 job 完成后能把结果写回进度区域。
6. 账户页自选组合、估值刷新、初始化账户等表单提交后不保留临时输入。
7. 账户页买入、卖出、调整成本继续清空临时输入。
8. 不同页面之间的 `symbol`、`symbols`、`watchlist_name` 不互相污染。

这一阶段不改生产代码，只让测试明确失败点。

### 阶段 2：统一恒温器 job/progress 流程

目标是让所有恒温器运行来源进入同一套进度流程。

计划动作：

1. 恒温器运行按钮统一提交到 job 入口。
2. job 内部继续调用现有 `handle_thermostat()`。
3. 保持 `handle_thermostat()` 可被测试和其他内部调用直接使用。
4. `render_job_progress()` 继续作为统一进度组件。
5. 普通来源和龙虎榜来源的完成结果都通过同一套 `job_status_payload()` 返回。

这样可以复用现有 job 机制，避免引入前端框架或大规模异步重写。

### 阶段 3：表单状态隔离与清理

目标是把“页面配置状态”和“操作临时输入”分开。

计划动作：

1. 定义哪些字段属于配置字段，哪些字段属于临时字段。
2. POST 成功后只把配置字段写回页面状态。
3. 针对账户页各操作生成清理后的显示表单。
4. 自选组合操作保留账户路径和当前有效组合选择，但清空本次输入。
5. 删除组合后，如果当前选中组合已不存在，则选择剩余第一个组合或显示空状态。
6. 回测页、恒温器页、账户页的状态不再无差别共用同一组字段。

此阶段不改变任何账户文件格式，只改变页面回显状态。

### 阶段 4：账户路径一致性

目标是消除用户可见的 `path` / `account_path` 分裂。

计划动作：

1. 页面展示仍可以用“账户路径”这个概念。
2. 恒温器页读取现金和持仓时使用页面显示的账户路径。
3. 账户页各表单使用同一个账户路径值。
4. GET 切换页面时，不因内部字段名不同读到另一个账户。

如果内部仍需要兼容 `path` 和 `account_path`，也应在边界处转换，用户不应感知差异。

### 阶段 5：来源选项和提示文案整理

目标是消除误导性 UI。

计划动作：

1. 检查股票池来源下拉是否只展示实际可运行来源。
2. 如果“同花顺龙虎榜”没有独立可用链路，则不作为主下拉选项展示。
3. 如果保留相关文案，必须说明实际使用来源或降级行为。
4. 调整“现金不足”相关用户可见文案：
   - 账户总现金不足时，显示账户现金不足。
   - 建议仓位金额不足一手时，显示建议仓位金额不足或试探仓金额不足。

### 阶段 6：回归验证

完成后运行 Web 相关测试和必要策略回归测试。

验证重点：

1. Web 表单行为符合规格。
2. 普通恒温器和龙虎榜来源进度体验一致。
3. 账户页操作不会污染其他表单。
4. 现有策略测试仍通过。
5. 不出现“未翻译字段”。
6. Git 工作区没有无关临时文件。

## 测试策略

### 必须新增或调整的测试

1. `tests/test_web_app.py`
   - 验证所有恒温器来源提交到统一 job/progress。
   - 验证 job 完成结果可以渲染回页面。
   - 验证 job 失败错误可以展示。
   - 验证账户页各操作成功后的表单字段清理。
   - 验证跨页面字段不污染。
   - 验证账户路径语义一致。
   - 验证来源选项不误导。
   - 验证用户可见字段不出现“未翻译字段”。

2. `tests/test_thermostat_strategy.py`
   - 仅在策略层文案需要调整时，增加现金/试探仓文案回归测试。

### 必跑测试

1. `.\.venv\Scripts\python.exe -m pytest tests/test_web_app.py -q`
2. `.\.venv\Scripts\python.exe -m pytest tests/test_thermostat_strategy.py -q`
3. `.\.venv\Scripts\python.exe -m pytest -q`

### 手动检查

建议实现后手动打开本地页面检查：

1. 手动输入运行恒温器，有进度和结果。
2. 自选组合运行恒温器，有进度和结果。
3. 市场范围运行恒温器，有进度和结果。
4. 龙虎榜运行恒温器，有同样进度和结果。
5. 账户页创建组合、添加股票、删除股票、重命名、刷新估值后输入框清空。
6. 页面窄屏下进度区和表单不明显重叠。

## 风险点

### 1. 当前 `examples/web_app.py` 文件较大

风险：在一个大文件里修改路由、表单状态、进度组件，容易引入无关回归。

控制方式：

- 只改 Web 工作台相关函数。
- 不顺手重构策略、账户存储或数据源。
- 每一组行为用测试锁定。

### 2. `LAST_FORM` 可能被多个页面依赖

风险：简单清空全局状态可能破坏用户期望的配置保留。

控制方式：

- 不做“一刀切全部清空”。
- 明确区分配置字段和临时字段。
- 只清理临时输入，保留账户路径、来源、日期范围、数据源等配置。

### 3. 普通恒温器改为 job 后结果返回方式变化

风险：原同步运行页面直接返回结果，改为 job 后测试或用户操作路径会变化。

控制方式：

- 保留 `handle_thermostat()` 作为内部直接运行函数。
- 只统一页面提交体验。
- Web 测试覆盖 job 完成结果渲染。

### 4. 账户路径字段兼容

风险：`path` 和 `account_path` 当前都存在，直接改名可能破坏已有表单。

控制方式：

- 用户可见统一，内部兼容过渡。
- 在边界处转换，不改变账户文件结构。

### 5. 现金不足文案来源不确定

风险：文案可能来自策略层，也可能来自 Web 翻译层。

控制方式：

- 先用测试定位文案来源。
- 能在 Web 层准确展示就不改策略。
- 只有策略层直接生成误导文案时，才做最小文案调整。

## 为什么这个方案不过度复杂

1. 复用现有 job/progress 机制，不引入新前端框架。
2. 保留 `handle_thermostat()`，不重写策略运行入口。
3. 主要修改集中在 `examples/web_app.py` 和 `tests/test_web_app.py`。
4. 表单状态只做规则化清理，不引入数据库或新持久化结构。
5. 账户路径只统一用户可见语义，不改变账户文件格式。
6. 股票池来源只做一致性整理，不新增数据源。
7. 策略计算逻辑保持不变，降低回归风险。

## 方案自查

### 是否真正解决 spec.md 核心问题

是。计划覆盖了统一进度、表单清理、状态隔离、账户路径一致性、来源选项一致性和提示文案区分。

### 哪些行为保持不变

策略计算、账户数据结构、数据源、回测核心结果、自选组合语义、页面顶部导航均保持不变。

### 测试是否清晰

清晰。主要测试集中在 `tests/test_web_app.py`，只在必要时补策略文案测试，并保留全量 pytest 作为最终回归。

### 是否引入过重复杂度

没有。方案复用现有 job/progress 能力和现有 Web 页面结构，不引入新框架、不拆数据库、不重写核心策略。

### 更稳的实现路径

先写失败测试，再统一 job 入口，再处理表单状态，最后处理文案和来源选项。这个顺序先锁住用户可见问题，再做最小范围修改，风险较低。
