# 工作台 UI 重构实施方案

> 基于当前 `spec.md`。本文件只描述方案、模块影响、测试策略、风险控制和复杂度边界，不写代码，不拆具体任务。

## 1. 目标

把当前本地 Web 从“长表单堆叠”调整为“工作台”式体验：

- 顶部仍保留“恒温器策略 / 回测诊断 / 账户”三个主入口。
- 恒温器策略页只展示当前选择相关字段，低频配置默认收起。
- 股票池的选择、临时编辑和账户自选组合管理职责分离。
- 账户页改为“账户概览 + 功能操作区”，避免所有表单纵向堆在一个页面。
- 只改变 UI 展示、表单组织和页面交互路径，不改变策略、回测、账户、行情、执行辅助和旧 CLI 的核心行为。

## 2. 总体方案

采用“保留现有本地 Web 架构，重排页面信息架构”的方案。

核心原则：

- 不引入新前端框架。
- 不重写恒温器策略、海龟系统、筛选引擎、账户模型或行情 provider。
- Web 正常路径继续只暴露恒温器策略、回测诊断和账户。
- 旧 CLI、旧筛选引擎、旧海龟源码保留为兼容代码，但不进入默认 Web 主流程。
- 页面通过条件展示、tabs、折叠区、弹窗/抽屉等方式减少默认可见字段。
- 对进入策略和账户逻辑的输入保持兼容，避免 UI 重构影响计算结果。

## 3. 修改哪些模块

### 3.1 本地 Web 主页面

主要影响 `examples/web_app.py`。

需要调整：

- 顶部导航和页面容器布局。
- 恒温器策略页渲染结构。
- 账户页渲染结构。
- 页面内表单提交后的提示、摘要、空状态和错误状态展示。
- Web 正常路径中旧策略列表、旧默认技术筛选、旧海龟入口的可见性。

保持边界：

- Web 启动方式保持不变。
- 路由和请求处理尽量沿用现有结构。
- 只在必要处拆出渲染辅助，不做大型 Web 架构迁移。

### 3.2 恒温器策略页面

主要影响 `examples/web_app.py` 中恒温器页面和提交处理。

需要调整：

- 股票池来源选择改为条件展示。
- 手动输入来源只在主页面显示“编辑手动股票池”入口，实际输入放入弹窗、抽屉或等价编辑区。
- 自选股组合来源从账户已有组合读取，不再手填组合名。
- 市场范围来源改为多选，支持沪深 A 股、沪市、深市、创业板、科创板、北交所等范围。
- 龙虎榜来源只默认显示时间范围，开始日期和结束日期只在“自定义”时显示。
- 新增策略日期范围选择，固定范围自动计算实际开始/结束日期。
- 可用现金从账户状态只读展示，账户未初始化时显示提示。
- 模拟资金只作为临时测算选项，启用后才显示输入框。
- 历史源、股票列表源、实时源、强制刷新、执行计划、溢价限制、成交量限制等低频字段收进高级设置。
- 删除恒温器策略页单独的“保存手动股票池”区域，把保存和管理职责统一到账户页。

保持边界：

- 恒温器策略仍消费最终股票代码列表。
- 不改变市场状态识别、个股状态识别、策略路由、网格/趋势/持仓建议等计算逻辑。
- 不改变策略运行结果的数据结构和含义。

### 3.3 回测诊断页面

主要影响 `examples/web_app.py` 中回测诊断页面的外层布局。

需要调整：

- 纳入统一工作台页面容器、标题区、内容分区和视觉层级。
- 保持回测输入、运行入口、结果展示和错误提示可读。
- 避免在回测诊断页重新暴露旧策略列表、旧默认技术筛选或旧海龟入口。

保持边界：

- 不改变回测诊断的参数语义。
- 不改变回测诊断的计算过程。
- 不改变回测诊断输出数据的含义。
- 不把回测诊断页扩展为新的策略管理入口。

### 3.4 股票池和日期范围辅助能力

可能影响 `stock_picker/pools.py` 以及 Web 层已有股票池调用。

需要调整：

- 支持手动股票池的解析预览：逗号、空格、换行分隔，显示识别数量、重复代码和无效代码。
- 支持市场范围多选后的合并、去重和摘要。
- 支持龙虎榜固定时间范围到实际日期范围的转换。
- 支持策略日期固定范围到实际日期范围的转换。
- 为 Web 页面提供可展示的股票池摘要、来源、数量和错误/警告信息。

保持边界：

- 不改变股票池最终语义：解析、去重、过滤、错误提示和来源追踪仍以现有规则为准。
- 不新增股票打分或投资判断。
- 不替换现有股票列表、龙虎榜或行情数据来源。

### 3.5 账户与自选组合

主要影响 `stock_picker/user/watchlist.py`、`stock_picker/user/portfolio.py` 和 `examples/web_app.py` 中账户页。

需要调整：

- 账户页增加或重排自选组合管理入口。
- 自选组合管理统一承担创建、删除、重命名、添加股票、删除股票、查看组合、从手动输入保存为组合等职责。
- 恒温器策略页只读取或跳转到该能力，不再重复实现保存区域。
- 账户概览改为卡片式展示。
- 持仓和交易流水默认展示摘要，交易流水默认最近 5 条，完整内容通过“查看全部”进入。
- 初始化账户、刷新行情/更新估值、手动买卖、调整成本等操作进入 tabs、折叠区、抽屉或二级区域。

保持边界：

- 自选组合不是持仓。
- 删除自选组合或组合内股票不得影响现金、持仓、交易流水、成本、盈亏统计或历史行情缓存。
- 账户初始化、买入、卖出、成本调整、估值刷新和盈亏统计逻辑保持不变。

### 3.6 页面样式和交互层

主要影响 `examples/web_app.py` 内现有 HTML/CSS 输出。

需要调整：

- 页面最大宽度和内容留白。
- 卡片、表单 grid、tabs、折叠区、弹窗/抽屉等视觉组织。
- 主操作和次要操作按钮样式区分。
- checkbox 与文本水平对齐。
- 空状态文案和下一步提示。
- 窄屏下卡片、表单字段和按钮的换行规则。

保持边界：

- 不追求独立设计系统。
- 不引入构建步骤。
- 不把 UI 工作扩展为完整前端重写。

### 3.7 文档和验证说明

可能影响 `README.md` 和已有测试说明。

需要调整：

- 说明 Web 正常入口仍是恒温器策略、回测诊断、账户。
- 说明自选组合管理在账户页完成。
- 说明恒温器策略页的手动股票池保存路径。
- 说明模拟资金只影响临时策略测算，不改变账户。
- 说明旧 CLI、旧筛选引擎、旧海龟保留但不在正常 Web 主流程展示。

## 4. 哪些行为保持不变

必须保持不变的核心行为：

- 恒温器策略的市场状态识别、个股状态识别、策略路由、候选生成、持仓建议、网格建议和趋势建议。
- 回测诊断的计算语义和输出含义。
- 账户初始化、买入、卖出、成本调整、持仓估值、交易流水和盈亏统计。
- 行情抓取渠道、数据源选择、fallback、缓存和错误记录。
- 执行辅助中的资金、手数、涨跌停、报价可用性和流动性检查。
- 股票池解析、去重、过滤、错误提示和来源追踪语义。
- 本地 Web 启动方式。
- 旧 CLI、旧筛选引擎、旧海龟源码继续保留为兼容代码。
- 页面优化不得改变海龟系统计算结果。

Web 可见性方面保持或收窄：

- 顶部正常入口仍只有恒温器策略、回测诊断、账户。
- 旧策略列表、旧默认技术筛选和旧海龟系统入口不在正常 Web 使用路径中展示。

## 5. 测试策略

### 5.1 先写或先补验证点

实施时先补覆盖 UI 行为的测试，再改页面：

- 恒温器策略页不同股票池来源下，只显示对应字段。
- 手动输入来源不在主页面显示普通输入框。
- 自选组合来源不显示手填组合名，并能显示空状态。
- 市场范围支持多选并展示摘要。
- 龙虎榜固定时间范围不显示开始/结束日期，自定义时才显示。
- 策略日期固定范围不显示开始/结束日期，自定义时才显示。
- 可用现金从账户只读展示，账户未初始化时显示提示。
- 模拟资金输入只在启用模拟资金时出现。
- 高级数据源和执行设置默认收起。
- 恒温器策略页不再显示“保存手动股票池”独立区域。

### 5.2 Web 集成测试

重点覆盖 `tests/test_web_app.py`：

- 顶部导航只包含三个正常主入口。
- 正常 Web 路径不出现旧策略列表、旧默认技术筛选或旧海龟入口。
- 回测诊断页仍可访问，并且只做工作台外层分区和视觉整理，不改变回测结果语义。
- 账户页顶部展示账户概览卡片。
- 账户未初始化时展示合理空状态和初始化入口。
- 当前持仓无数据时显示“暂无持仓”。
- 交易流水默认只显示最近 5 条，并提供“查看全部”入口。
- 账户功能区通过 tab、折叠、侧栏、弹窗或等价方式组织，不再全部纵向展开。
- 手动买入/卖出的高级字段默认收起。
- 成本调整默认不占据大面积页面。

### 5.3 股票池和日期范围测试

重点覆盖 `tests/test_stock_pools.py`、`tests/test_pool_market_ranges.py`、`tests/test_pool_lhb.py` 或等价测试：

- 手动输入支持逗号、空格、换行分隔。
- 能正确输出识别数量、重复代码和无效代码。
- 市场范围多选结果合并、去重和空选择提示正确。
- 龙虎榜时间范围包含最近 1 周、最近 1 个月、最近 3 个月、最近半年、最近 1 年、自定义。
- 策略日期范围包含最近 1 个月、最近 3 个月、最近半年、最近 1 年、自定义。
- 固定时间范围能稳定计算实际开始日期和结束日期。

### 5.4 账户和自选组合测试

重点覆盖 `tests/test_watchlist_store.py`、`tests/test_portfolio_journal.py` 或等价测试：

- 自选组合创建、删除、重命名、添加股票、删除股票、查看股票列表。
- 从手动输入保存为组合时复用账户页自选组合逻辑。
- 删除自选组合不影响账户持仓、现金、交易流水和行情缓存。
- 初始化账户后概览字段更新。
- 刷新估值后概览、持仓市值和浮动盈亏同步更新。

### 5.5 核心回归测试

必须保留并运行相关回归：

- 恒温器策略测试。
- 恒温器回测测试。
- Web 测试。
- 账户流水测试。
- 执行辅助测试。
- 海龟系统测试。
- 数据服务测试。
- 文档测试。

建议验证命令在任务阶段细化，但至少应覆盖：

- `.\.venv\Scripts\python.exe -m pytest tests\test_web_app.py -q`
- `.\.venv\Scripts\python.exe -m pytest tests\test_stock_pools.py tests\test_pool_market_ranges.py tests\test_pool_lhb.py tests\test_watchlist_store.py -q`
- `.\.venv\Scripts\python.exe -m pytest tests\test_thermostat_strategy.py tests\test_thermostat_backtest.py tests\test_turtle_system.py tests\test_portfolio_journal.py tests\test_execution.py -q`
- `.\.venv\Scripts\python.exe -m pytest -q`
- 本地 Web 启动后的 HTTP 200 和关键页面文本检查。

## 6. 风险点

### 6.1 UI 隐藏字段导致提交缺少必要参数

风险：

动态展示后，原本一直存在的表单字段可能在某些来源下缺失，导致后端接收不到默认值。

控制：

- 每个来源都有明确默认值和提交约束。
- 固定日期范围在提交前转换为实际日期。
- 高级设置收起但默认值仍可被提交或由服务端补齐。
- 用 Web 集成测试覆盖每种来源的提交路径。

### 6.2 页面重排误改策略或账户计算结果

风险：

调整表单字段和提交参数时，可能改变恒温器策略、账户交易或回测输入。

控制：

- 策略、账户、执行和回测核心逻辑不做重写。
- UI 层只负责展示、收集和转换输入。
- 用恒温器、账户、执行、海龟和回测回归测试锁定结果。

### 6.3 自选组合和持仓概念混淆

风险：

用户可能把自选组合理解为真实持仓，或以为删除自选组合会影响账户。

控制：

- 文案和页面分区明确“自选组合”属于账户页的用户配置。
- 持仓与自选组合在不同区域展示。
- 测试保护删除自选组合不影响账户状态。

### 6.4 市场范围多选带来大股票池性能压力

风险：

沪深 A 股等大范围可能导致策略运行耗时明显增加。

控制：

- 展示最终股票数量和大范围耗时提示。
- 不改变现有行情抓取和缓存策略。
- 大范围选择只改变输入股票池，不引入新的批量抓取架构。

### 6.5 龙虎榜和同花顺来源可用性不稳定

风险：

外部龙虎榜来源可能不可用，尤其同花顺数据可能受环境和访问限制影响。

控制：

- 页面必须显示真实数据来源。
- 不可用时提示真实原因和实际使用来源。
- 不把其他来源伪装成同花顺。
- 这部分作为来源说明和错误展示处理，不阻塞基础 UI 重构。

### 6.6 单文件 Web 代码继续变大

风险：

如果所有 UI 片段继续堆在 `examples/web_app.py`，维护难度会上升。

控制：

- 允许在现有架构内做轻量渲染辅助拆分。
- 不做大型前端框架迁移。
- 任务阶段按页面区域分批修改，避免一次改动过多文件。

### 6.7 视觉优化难以完全自动化验证

风险：

布局美观、窄屏可用和层级清晰不能完全靠单元测试验证。

控制：

- 用文本/结构测试验证关键可见性和默认展开状态。
- 用本地 Web HTTP 检查确认页面可访问。
- 完成阶段补充手动窄屏检查或截图检查结果。

## 7. 为什么这个方案不过度复杂

- 不引入新前端框架，不增加构建链路。
- 不重写策略、账户、行情、执行、回测或海龟系统。
- 不删除旧 CLI、旧筛选引擎或旧海龟源码，只把它们退出正常 Web 主流程。
- 重点是页面信息架构和条件展示，属于现有 Web 的重排和收敛。
- 自选组合管理统一到账户页，避免恒温器策略页重复一套保存逻辑。
- 恒温器策略页继续只向策略层传最终股票池和参数，保持边界简单。
- 低频操作通过 tabs、折叠、弹窗或二级入口收纳，不需要新增复杂业务模型。
- 测试策略以现有测试体系为基础，只补 UI 可见性、来源条件展示和回归保护。

## 8. 推荐实施顺序

1. 先补 Web 可见性和核心回归测试，锁定“哪些字段默认可见、哪些字段必须隐藏、哪些计算不能变”。
2. 先整理统一页面壳层、顶部导航和回测诊断页外层分区，验证回测诊断结果语义不变。
3. 调整恒温器策略页的股票池来源、日期范围、账户现金和高级设置展示。
4. 统一手动股票池保存入口，把恒温器页重复区域移到账户页职责内。
5. 重构账户页为概览卡片加功能操作区。
6. 补齐自选组合管理、持仓摘要、交易流水最近 5 条和“查看全部”入口。
7. 做视觉层级、grid 布局、按钮位置、空状态和窄屏可用性整理。
8. 运行 Web、股票池、账户、策略、回测、海龟、执行和全量回归验证。

## 9. 自查

- 覆盖了 `spec.md` 的核心问题：字段过多、职责混乱、账户页过长、状态同步、表单层级弱。
- 明确了需要修改的模块。
- 明确了哪些核心行为保持不变。
- 明确了测试策略和回归范围。
- 明确了主要风险和控制方式。
- 方案没有写代码，也没有进入任务拆分。
- 方案没有要求重写旧 CLI、旧筛选引擎、旧海龟源码。
- 已补充回测诊断页的轻量工作台分区边界，避免后续只重构恒温器策略页和账户页而遗漏第三个主入口。
