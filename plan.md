# 自选组合输入规范化与校验实施方案

> 基于当前 `spec.md`。本文件只描述方案、模块影响、测试策略、风险和复杂度边界，不写代码，不拆具体实现任务。

## 1. 目标

修复账户页“自选组合”输入处理不一致的问题：同一批股票代码在不同入口下必须按一致规则拆分、校验、规范化、去重和反馈，避免未经校验的原始字符串进入自选组合或恒温器策略。

目标完成后：

- 用户批量输入股票代码时，系统不会把整段文本保存为一个股票。
- 合法且当前系统支持的代码保存为统一格式。
- 无效或当前系统不支持的代码在保存前提示，不会静默写入。
- 历史遗留异常自选组合不会继续污染策略运行。
- 自选组合相关修复不改变账户资产、行情渠道和恒温器计算逻辑。

## 2. 总体方案

采用“统一股票代码解析入口 + 自选组合存储前校验 + Web 结果反馈 + 策略读取防护”的轻量方案。

核心思路：

- 将自选组合添加股票时的输入处理对齐到现有股票池解析语义，避免账户页和手动股票池各走一套规则。
- 自选组合持久化层不再接受未经校验的原始股票字符串。
- Web 层负责显示新增、重复、无效、不支持和历史异常数据的反馈。
- 策略读取自选组合时复用股票池校验结果，保证异常条目不会进入行情查询。
- 不新增数据源，不新增证券类型支持，不改策略计算。

该方案重点修复数据进入系统的边界，而不是在策略报错后再补救。

## 3. 修改哪些模块

### 3.1 股票池解析与校验：`stock_picker/pools.py`

计划调整职责：

- 作为手动输入和自选组合批量输入的共同解析规则来源。
- 支持规格要求的分隔符：英文逗号、中文逗号、空格、换行、分号。
- 明确“当前系统支持的证券代码”判定，避免 6 位但无法映射到支持市场后缀的代码被当作可用股票。
- 对无法识别、当前不支持、重复、过滤后的结果提供可被 Web 层展示的结构化信息。

保持边界：

- 不改变股票池结果对象的业务含义。
- 不改变市场范围、龙虎榜和手动输入股票池的既有计算语义。
- 不新增 ETF、基金、可转债等代码支持。

### 3.2 自选组合存储：`stock_picker/user/watchlist.py`

计划调整职责：

- `WatchlistStore` 保存股票前必须只接受已校验、已规范化的股票列表，或在内部拒绝明显非法输入。
- 添加股票的结果需要能表达：新增项、重复项、无效项、当前不支持项、最终组合数量。
- 删除股票时接受用户常见输入形式，但删除动作仍以规范化代码为准。
- 已存在组合继续可读，不能因为历史数据里有异常字符串导致整个组合无法打开。

保持边界：

- 不改变自选组合“不是持仓”的业务含义。
- 不改变账户现金、持仓和交易流水。
- 不破坏现有 `watchlists.json` 的读取兼容性。

### 3.3 Web 账户页：`examples/web_app.py`

计划调整职责：

- 账户页“添加股票”入口使用与手动股票池保存一致的解析和校验规则。
- 添加股票结果显示新增数量、重复数量、无效或不支持代码列表、最终组合数量。
- 自选组合表格展示时识别历史异常条目，给出可见警告。
- 自选组合中存在异常条目时，页面提示用户清理或重新添加，不让用户误以为组合完全可用。
- 删除股票、重命名、删除组合等操作继续保留原功能，只补充必要结果反馈。

保持边界：

- 不重构账户页整体布局。
- 不改变路由含义和表单的核心业务目的。
- 不把自选组合变成持仓管理。

### 3.4 恒温器读取自选组合：`examples/web_app.py` 与 `stock_picker/pools.py`

计划调整职责：

- 恒温器策略选择“自选股组合”时，通过统一股票池结果读取有效股票。
- 历史异常条目不得进入行情查询。
- 如果组合最终没有可用股票，页面显示自选组合无有效股票或存在异常代码的中文提示。

保持边界：

- 不改恒温器市场状态判断。
- 不改个股状态、网格建议、趋势建议、新买候选、持仓建议和资金分配。
- 不改行情服务调用策略，只保证传入符号是已校验的有效股票。

### 3.5 测试模块

计划修改或补充：

- `tests/test_stock_pools.py`：覆盖分隔符、批量输入、无效代码、不支持代码、重复代码、历史异常条目解析。
- `tests/test_watchlist_store.py`：覆盖自选组合添加、去重、拒绝原始组合字符串、删除规范化代码、账户资产不受影响。
- `tests/test_web_app.py`：覆盖账户页添加股票表单、结果反馈、自选组合表格展示、恒温器读取异常组合时不把异常条目交给行情查询。

不需要新增测试框架。

## 4. 哪些行为保持不变

必须保持不变：

- 行情、历史数据、龙虎榜、股票列表等信息渠道不变。
- 恒温器策略计算逻辑不变。
- 市场状态、个股状态、候选排序、网格建议、趋势建议和资金分配不变。
- 账户现金、持仓、交易流水、买入、卖出、成本调整和估值逻辑不变。
- 自选组合仍然只是股票池来源，不是账户持仓。
- 现有 `watchlists.json` 可继续读取。
- CLI 和旧筛选逻辑不因本次修复被重写。
- 不新增 ETF、基金、可转债等证券类型支持。

允许改变：

- 自选组合添加股票时的输入校验更严格。
- 以前会静默保存的无效字符串，现在会被拒绝或提示。
- 自选组合表格中会显示历史异常数据警告。
- 使用异常自选组合运行策略时，页面会更早显示可理解的错误或警告。

## 5. 测试策略

### 5.1 先写失败测试

实施时先补测试，让当前问题以失败测试形式暴露：

- 账户页添加 `600519, 000001 300750` 时不应保存为一个整体字符串。
- 账户页添加换行分隔代码时应按多个股票处理。
- 账户页添加 `516650,515880,515070,159801` 时不应保存原始整段文本。
- 不支持代码应在保存前反馈，不应在运行策略时才由行情接口报错。
- 已有异常组合被恒温器读取时，异常条目不进入行情查询。

### 5.2 分层测试

股票池解析层：

- 验证所有分隔符。
- 验证合法代码规范化。
- 验证重复代码去重。
- 验证无效和不支持代码被记录。
- 验证空输入产生错误。

自选组合存储层：

- 验证新增、重复、删除、重命名、删除组合。
- 验证不保存原始组合字符串。
- 验证历史异常数据仍可读取但会被识别。
- 验证自选组合操作不影响账户资产。

Web 层：

- 验证账户页自选组合操作结果有可见反馈。
- 验证表格不再把多代码原始字符串显示为单个有效股票。
- 验证恒温器读取异常组合时显示错误或警告。
- 验证正常自选组合仍可运行。

回归层：

- 验证恒温器结果、账户结果、股票池结果没有被无关改动影响。

### 5.3 建议运行命令

定向测试：

- `.\.venv\Scripts\python.exe -m pytest tests\test_stock_pools.py -q`
- `.\.venv\Scripts\python.exe -m pytest tests\test_watchlist_store.py -q`
- `.\.venv\Scripts\python.exe -m pytest tests\test_web_app.py -q`

相关回归：

- `.\.venv\Scripts\python.exe -m pytest tests\test_thermostat_strategy.py tests\test_execution.py -q`

完整回归：

- `.\.venv\Scripts\python.exe -m pytest -q`

### 5.4 手动检查

完成后建议手动检查：

- 打开账户页，自选组合中添加 `603309`，确认保存为统一格式。
- 添加 `600519, 000001 300750`，确认拆成多个代码。
- 添加 `516650,515880,515070,159801`，确认不会保存整段原始文本。
- 使用包含异常历史数据的组合运行恒温器，确认页面显示警告或错误，且不再出现 BaoStock “股票代码应为9位”的下游错误。
- 确认账户现金、持仓和交易流水未因自选组合操作改变。

## 6. 风险点

### 6.1 支持代码范围判断不清

风险：
6 位数字不一定都是当前系统支持的股票代码。若只检查长度，仍可能保存无法转换成行情接口格式的代码。

控制：
明确“可规范化到支持市场后缀”的代码才允许作为有效股票保存；当前不支持的类型必须提示，不得原样保存。

### 6.2 Web 层和存储层重复校验

风险：
如果 Web 层和 `WatchlistStore` 各写一套规则，后续会再次出现入口不一致。

控制：
将拆分、校验和规范化规则集中复用，Web 层只负责展示结果。

### 6.3 历史异常数据处理过度

风险：
自动批量修改历史 `watchlists.json` 可能误删用户数据。

控制：
第一阶段以识别、排除、提示为主，不静默批量修复全部历史文件。需要用户确认的数据修复可后续单独做。

### 6.4 过度拦截用户真实想用的证券类型

风险：
用户输入的 5xxxxx 可能是 ETF 或基金，但当前规格明确不新增这类支持。

控制：
提示“当前暂不支持”，不要伪装成股票保存。后续如需 ETF/基金支持，另开规格补数据源、行情和策略兼容。

### 6.5 影响现有手动股票池逻辑

风险：
统一解析规则时可能误改手动输入股票池、龙虎榜或市场范围行为。

控制：
只扩展共享解析能力和自选组合入口校验，保留现有股票池结果语义，并用 `tests/test_stock_pools.py` 做回归。

### 6.6 策略层仍可能收到异常符号

风险：
若只修账户页保存，不处理历史异常组合，旧数据仍会进入策略。

控制：
恒温器读取自选组合时必须经过股票池结果校验，历史异常条目不得进入行情查询。

## 7. 为什么这个方案不过度复杂

- 不引入新框架。
- 不新增数据库或迁移系统。
- 不重写账户页、恒温器策略、行情服务或选股引擎。
- 不扩展证券类型支持范围。
- 复用现有股票池解析和自选组合存储结构。
- 只在输入边界、存储边界和策略读取边界补一致性检查。
- 测试集中在现有 `pytest` 测试文件中，不新增复杂测试工具。
- 历史异常数据第一阶段只识别和阻断污染，不做大规模自动修复。

## 8. 更稳的实现路径

推荐顺序：

1. 先补股票池解析层测试，锁定分隔符、无效代码、不支持代码和去重行为。
2. 再补自选组合存储层测试，锁定不保存原始组合字符串和账户资产不受影响。
3. 再补 Web 层测试，锁定账户页添加股票结果反馈。
4. 再补恒温器读取历史异常组合的测试，锁定异常条目不进入行情查询。
5. 按测试失败点最小化修改共享解析和自选组合入口。
6. 跑定向测试。
7. 跑恒温器和账户相关回归。
8. 跑完整测试。
9. 做一次本地页面手动检查。

这条路径先保护最底层规则，再扩展到 Web 和策略读取，能降低一次性改太多文件的风险。

## 9. Planning/Review 审查结论

- 是否真正解决核心问题：是。方案聚焦自选组合输入入口不一致、原始组合字符串被保存、异常数据进入策略这三个核心问题。
- 是否有清晰测试方案：是。测试分为股票池解析层、自选组合存储层、Web 层、恒温器读取层和完整回归。
- 是否引入过重复杂度：否。方案不引入新框架、不改数据源、不重写策略，只补输入边界和读取边界。
- 是否有更稳实现路径：有。推荐先测试共享解析规则，再测试存储，再测试 Web，最后测试策略读取历史异常数据。
- 建议：实施阶段应优先复用现有股票池解析语义，避免在 Web 表单里单独手写一套校验规则。
