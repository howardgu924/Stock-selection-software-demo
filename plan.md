# 优化汉化实施方案

> 基于当前 `spec.md`。本文件只描述方案、模块影响、测试策略、风险控制和复杂度边界，不写代码，不拆具体任务。

## 1. 目标

解决本地 Web 工作台在运行系统后仍出现英文的问题。正常 Web 使用路径中，除股票代码、数据源品牌名、路径、URL、外部异常原文等规格允许保留的内容外，用户可见的页面文案、结果区标题、表格列名、状态值、动作值、错误提示、空状态和进度提示应显示为中文。

本次只做展示层汉化，不改变信息渠道、内部逻辑、接口语义、持久化数据结构、策略计算、账户计算、回测计算和 CLI 兼容性。

## 2. 总体方案

采用“集中展示映射 + 渲染出口统一转换 + 回归测试保护”的方案。

核心思路：

- 先盘点当前正常 Web 路径中的英文泄漏点，形成实施时的检查清单，避免只修复截图里出现的字段。
- 保留现有本地 Web 架构和 `examples/web_app.py` 的主要请求处理流程。
- 以现有 `TITLE_LABELS`、`COLUMN_LABELS`、`OPTION_LABELS` 为基础补齐用户可见中文映射。
- 把英文标题、英文列名、英文枚举值的转换控制在 Web 展示出口，不改策略层、账户层、股票池层、执行辅助层返回的数据结构。
- 对未知列名采用可见的中文兜底提示，避免英文静默漏出。
- 对错误和外部异常采用“中文摘要 + 原始详情”的展示方式。
- 对进度提示采用中文阶段名和中文节点描述，不直接暴露内部 `stage`。
- 通过测试锁定“展示中文”和“计算结果不变”两个目标。

该方案刻意避免全局搜索替换、字段重命名或跨层改数据结构，降低破坏策略和账户逻辑的风险。

更稳的实施路径是“清单化发现 -> 测试锁定 -> 展示出口修复 -> 回归验证”：

1. 先用现有页面和测试样本列出英文区块标题、英文列名、英文枚举值、英文进度阶段。
2. 再为这些泄漏点补 Web 可见性测试，让当前问题先暴露为测试失败。
3. 然后只改展示映射和渲染出口，不碰业务层字段。
4. 最后用回归测试证明计算结果和内部结构没有变化。

## 3. 修改哪些模块

### 3.1 Web 展示层：`examples/web_app.py`

主要修改模块。

计划调整：

- 补齐结果区标题映射，包括恒温器、龙虎榜、回测诊断、账户、持仓、交易流水、自选组合、执行计划等区块。
- 补齐表格列名映射，包括 `watchlist_name`、`time_range`、`source_detail`、`market_regime`、`confidence`、`data_source`、`data_sufficient`、执行计划字段、回测字段和账户字段。
- 补齐状态值和枚举值映射，包括市场状态、个股状态、策略类型、建议动作、置信度、布尔值、股票池来源、数据来源等。
- 调整表格渲染出口，确保列名统一走中文显示。
- 调整表格单元格显示出口，确保枚举值在用户页面显示为中文。
- 调整摘要渲染出口，确保摘要字段名和摘要值汉化。
- 调整结果区标题渲染出口，确保 `TableBlock` 的英文内部标题不会直接显示给用户。
- 调整进度条展示，确保阶段名、节点说明和失败提示显示中文。
- 调整错误、警告和空状态展示，确保有中文摘要；必要时保留原始异常详情。

保持边界：

- 不改 `TableBlock` 表示的业务含义。
- 不改 POST/GET 路由语义。
- 不改表单提交字段名。
- 不改策略、账户、回测、执行辅助的输入输出结构。
- 不通过机械替换改变内部字段名。

### 3.2 恒温器策略相关展示

主要影响 `examples/web_app.py` 中恒温器运行结果、龙虎榜候选、手工执行计划和进度展示。

计划调整：

- 汉化恒温器结果区标题：股票池摘要、市场概览、持仓建议、新买候选、网格建议、趋势建议、手工执行计划、错误。
- 汉化恒温器结果列名和状态值。
- 汉化龙虎榜 Top N 结果标题，例如“龙虎榜前 20 名”。
- 汉化进度提示，例如“正在加载候选股历史”“正在评估恒温器”“正在生成手工执行计划”。
- 保留股票代码、数据源品牌名和外部异常原文。

保持边界：

- 不改变龙虎榜候选池生成、Top N、排序、去重、过滤规则。
- 不改变恒温器市场状态和个股状态判断。
- 不改变网格、趋势、持仓建议和执行计划计算结果。

### 3.3 回测诊断相关展示

主要影响 `examples/web_app.py` 中回测诊断结果展示。

计划调整：

- 汉化回测结果区标题：摘要、市场状态表现、诊断明细、回测股票池、回测候选差异、年度收益、月度收益、月度收益矩阵、交易质量、持仓分布、回撤、个股盈亏、交易记录、权益曲线、错误。
- 汉化回测表格列名和常见状态值。
- 保留股票代码、路径、数据源品牌名和外部异常详情。

保持边界：

- 不改变回测计算逻辑。
- 不改变回测统计口径。
- 不改变回测输出数据结构。

### 3.4 账户和自选组合相关展示

主要影响 `examples/web_app.py` 中账户页、账户概览、持仓、交易流水和自选组合结果展示。

计划调整：

- 汉化账户结果区标题：账户概览、当前持仓、交易记录、自选组合等。
- 汉化账户表格列名：本金、现金、持仓市值、总资产、已实现盈亏、浮动盈亏、佣金率、印花税率、成交价、股数、原因、备注等。
- 汉化空状态和错误提示，例如“暂无持仓”“暂无交易流水”“暂无自选组合”“账户未初始化”。

保持边界：

- 不改变账户初始化、买入、卖出、成本调整、估值刷新逻辑。
- 不改变持仓、现金、交易流水和盈亏统计。
- 不改变自选组合存储结构。

### 3.5 股票池和执行辅助展示

可能影响 `examples/web_app.py` 中股票池摘要、执行计划展示，以及测试中对展示结果的断言。

计划调整：

- 汉化股票池摘要字段和来源值。
- 汉化执行计划字段和动作值，例如推荐操作、备选操作、涨跌停状态、资金不足跳过、成交量限制跳过。
- 对执行计划无法生成的情况提供中文空状态或中文错误摘要。

保持边界：

- 不改变股票池解析、去重、过滤和来源追踪语义。
- 不改变执行辅助中的资金、手数、涨跌停、报价可用性和流动性检查。

### 3.6 测试模块

主要影响现有测试，不新增复杂测试框架。

计划调整：

- 修改或新增 `tests/test_web_app.py` 中的用户可见页面断言。
- 按需补充恒温器、龙虎榜、回测、账户结果的 HTML 文本断言。
- 保留现有策略、账户、回测和执行辅助测试，用于证明计算结果没有被汉化改动影响。
- 如果测试中需要断言内部字段名，应明确区分“内部数据结构仍是英文键名”和“Web 页面展示为中文”。

保持边界：

- 不要求 CLI 输出全面汉化。
- 不要求内部单元测试的 DataFrame 字段名改中文。
- 不因为页面汉化而修改底层测试数据结构。

## 4. 哪些行为保持不变

必须保持不变：

- 行情、龙虎榜、股票列表、指数、实时行情、账户和历史数据的信息渠道。
- 数据源选择、fallback、缓存、刷新和错误记录逻辑。
- 恒温器策略的市场状态识别、个股状态识别、策略路由、持仓建议、新买候选、网格建议、趋势建议和手工执行计划计算结果。
- 龙虎榜候选池生成、Top N 选择、去重、过滤和排序规则。
- 回测诊断的计算逻辑、统计口径和结果数据。
- 账户初始化、买入、卖出、成本调整、估值刷新、持仓和交易流水逻辑。
- 股票池解析、去重、过滤、剔除科创板和错误提示语义。
- CLI 参数、内部字段名、测试数据结构和持久化数据格式。
- 本地 Web 启动方式。

允许改变：

- 用户页面上的标题文案。
- 用户页面上的表格列名。
- 用户页面上的状态值显示文案。
- 用户页面上的错误、警告、空状态和进度提示。
- 用户页面上未知字段的兜底展示方式。

## 5. 测试策略

### 5.1 先补可见性测试

实施时应先补或调整测试，让当前英文泄漏能被测试捕获。

重点测试：

- 恒温器策略运行结果页面不显示 `Stock Pool Summary`、`Market Overview`、`New Buy Candidates`、`Trend Advice`、`Execution Plan`、`Errors` 等英文区块标题。
- 页面显示“股票池摘要”“市场概览”“新买候选”“趋势建议”“手工执行计划”“错误”等中文标题。
- 表格列名不直接显示 `watchlist_name`、`time_range`、`source_detail`、`market_regime`、`confidence`、`data_source`、`data_sufficient`。
- 页面显示“自选组合名称”“时间范围”“来源说明”“市场状态”“置信度”“数据来源”“数据是否充足”。
- 枚举值显示为中文，例如“震荡区间”“趋势跟随”“观察”“买入”“等待确认”。
- 龙虎榜 Top N 标题显示为“龙虎榜前 20 名”等中文标题。
- 回测诊断和账户页不显示规格列出的英文区块标题。

测试注意：

- 不应写“页面不得包含任何英文字符”这类过宽断言，因为股票代码、数据源品牌名、路径、URL、命令行参数和外部异常原文允许保留英文。
- 禁止项应聚焦规格列出的英文标题、英文字段名和内部枚举值。
- 允许项应明确断言，例如 `600519.SH`、`akshare`、`data/user/default` 可以存在，但它们旁边的字段标签必须是中文。

### 5.2 测试渲染出口

应覆盖渲染出口，而不是只测某个单独页面。

重点测试：

- 表格渲染会把已知列名转为中文。
- 摘要渲染会把已知字段名转为中文。
- 表格值渲染会把已知枚举值转为中文。
- 未知列名显示“未翻译字段：原字段名”或等价中文提示。
- 外部异常显示时包含中文摘要，并保留原始详情。

### 5.3 测试进度提示

重点测试：

- 进度 payload 中的内部 `stage` 不直接作为用户可见文案展示。
- 有总数时显示“已完成 x / y”。
- 有当前股票时显示当前处理股票。
- 失败时显示中文失败摘要。

### 5.4 回归测试

汉化不应影响计算结果，因此必须运行现有回归：

- Web 测试：`.\.venv\Scripts\python.exe -m pytest tests\test_web_app.py -q`
- 恒温器和执行辅助：`.\.venv\Scripts\python.exe -m pytest tests\test_thermostat_strategy.py tests\test_thermostat_backtest.py tests\test_execution.py -q`
- 股票池和龙虎榜：`.\.venv\Scripts\python.exe -m pytest tests\test_stock_pools.py tests\test_pool_market_ranges.py tests\test_pool_lhb.py tests\test_lhb_candidates.py -q`
- 账户和自选组合：`.\.venv\Scripts\python.exe -m pytest tests\test_portfolio_journal.py tests\test_watchlist_store.py -q`
- 完整回归：`.\.venv\Scripts\python.exe -m pytest -q`

回归验证重点：

- 页面显示可以变为中文，但策略层返回的 DataFrame 字段名应保持原样。
- 同一输入下，恒温器候选数量、建议动作原始值、执行计划行数、回测汇总数值、账户现金和持仓统计不应因汉化改变。
- 如果某个测试需要同时检查内部数据和页面展示，应分成两个断言：内部字段保持英文键名，页面 HTML 显示中文标签。

### 5.5 手动验证

完成后建议手动检查：

- 打开本地 Web 首页，运行恒温器策略。
- 查看股票池摘要、市场概览、趋势建议、执行计划、错误区是否仍有不该出现的英文标题或字段名。
- 切换到龙虎榜来源，确认 Top N、进度和结果区中文显示。
- 打开回测诊断页，确认结果区标题中文。
- 打开账户页，确认持仓、交易流水、自选组合等中文显示。

## 6. 风险点

### 6.1 全局机械替换破坏内部字段

风险：

如果直接把英文字段名替换成中文，可能破坏 DataFrame 字段、测试断言、表单字段、持久化数据或策略逻辑。

控制：

- 只在 Web 展示出口做中文映射。
- 不重命名内部字段。
- 不改持久化数据结构。
- 用策略、账户、回测和执行辅助测试做回归保护。

### 6.2 映射遗漏导致页面仍漏英文

风险：

结果区块和字段分散在多个页面，容易漏掉某些标题或列名。

控制：

- 以 `spec.md` 第 6、7、8 节作为映射清单。
- 测试覆盖恒温器、龙虎榜、回测、账户四类页面。
- 对未知字段使用“未翻译字段”中文兜底，避免无声漏出。

### 6.3 过宽英文禁用规则误伤合法内容

风险：

如果测试或实现把所有英文字符都视为问题，会误伤股票代码、数据源品牌、路径、URL、命令行参数和外部异常详情。

控制：

- 测试只禁止规格列出的英文标题、英文字段名和内部枚举值直接展示。
- 允许保留英文的内容必须带中文标签或中文摘要。
- 外部异常原文只能作为详情保留，不能替代中文摘要。

### 6.4 错误详情过度汉化导致排查困难

风险：

外部异常如果被完全改写，后续难以定位数据源或库错误。

控制：

- 用户可见摘要中文化。
- 原始异常详情保留在中文摘要之后。
- 不翻译外部异常原文、路径、URL、库名和数据源品牌名。

### 6.5 枚举值翻译影响测试或导出

风险：

如果枚举值在数据层被改成中文，可能影响策略判断和测试。

控制：

- 内部仍使用原始枚举值。
- 只在 HTML 展示时转换。
- 测试同时覆盖内部字段不变和页面显示中文。

### 6.6 中文兜底影响页面可读性

风险：

如果大量未知字段显示“未翻译字段”，页面会显得粗糙。

控制：

- 先补齐规格列出的关键字段。
- 兜底只作为发现漏翻的保护机制。
- 后续根据测试或手动检查补齐遗漏映射。

### 6.7 进度提示来源不统一

风险：

进度信息来自不同阶段，某些阶段可能只有内部 stage，没有中文节点。

控制：

- 建立明确的阶段显示映射。
- 有 `node` 时优先展示中文节点。
- 无 `node` 时用中文阶段名兜底。
- 不直接把内部 stage 暴露给用户。

## 7. 为什么这个方案不过度复杂

- 不引入新前端框架。
- 不新增构建流程。
- 不重写策略、账户、回测、行情、股票池或执行辅助模块。
- 不改内部字段名和持久化结构。
- 复用现有 `TITLE_LABELS`、`COLUMN_LABELS`、`OPTION_LABELS` 思路，只补齐展示映射和统一出口。
- 测试基于现有 `pytest` 和 `tests/test_web_app.py`，不引入新的测试工具。
- 汉化范围限定在正常 Web 使用路径，不追求一次性翻译开发者日志和 CLI 原始输出。
- 通过中文兜底让漏翻可见，而不是搭建复杂的国际化系统。

## 8. 推荐实施顺序

1. 先盘点正常 Web 路径的英文泄漏清单，区分必须汉化和允许保留英文。
2. 先补 Web 可见性测试，让当前英文标题、英文列名和英文枚举值泄漏能被捕获。
3. 补齐结果区标题映射，优先覆盖恒温器、龙虎榜、回测和账户页面。
4. 补齐表格列名映射，优先覆盖规格列出的关键字段。
5. 补齐枚举值和状态值映射，优先覆盖市场状态、策略类型、建议动作、置信度和布尔值。
6. 统一摘要、表格标题、表格列名和表格值的展示出口。
7. 增加未知字段中文兜底，让漏翻可见。
8. 汉化进度提示和失败提示。
9. 汉化错误、警告和空状态摘要，并保留原始异常详情。
10. 运行 Web 定向测试。
11. 运行策略、账户、股票池、回测和执行辅助回归测试。
12. 运行完整测试。
13. 手动检查本地 Web 关键页面。

## 9. 自查

- 已覆盖 `spec.md` 的核心问题：运行系统后仍有英文。
- 已明确修改模块：主要是 `examples/web_app.py` 的展示层和 `tests/test_web_app.py` 等测试。
- 已明确保持不变：信息渠道、内部逻辑、接口语义、持久化结构、CLI 兼容性和计算结果。
- 已明确测试策略：先补可见性测试，再做展示映射，最后全量回归。
- 已明确风险点：机械替换、映射遗漏、过宽英文禁用、异常详情、枚举值、进度提示和中文兜底。
- 已说明不过度复杂：不引入 i18n 框架，不改底层数据结构，只做展示出口映射。
- 已补充更稳实现路径：先盘点英文泄漏清单，再用测试锁定，再改展示出口。
- 未写代码，未进入实现任务拆分。
