# 优化汉化规格

## 1. 背景和目标

当前本地 Web 工作台在运行系统后仍会出现英文，包括结果区标题、表格列名、状态值、操作建议、数据源字段名、错误区标题和部分说明文字。用户在查看恒温器策略、龙虎榜候选、回测诊断、账户结果和手工执行计划时，需要在中文页面中理解英文技术字段，影响阅读和执行判断。

本次目标是优化汉化体验：正常 Web 使用路径中，除本规格明确允许保留英文的内容外，面向用户展示的页面标题、表单标签、按钮、结果区标题、表格列名、状态值、空状态、错误提示、进度提示和说明文字必须使用中文。内部字段名、数据结构、信息渠道和策略逻辑保持不变。

本规格只描述用户可见文案、汉化范围、保留范围和验收标准，不规定具体代码结构、实现方案、函数设计或文件拆分。

## 2. 保持不变的范围

本次汉化优化不得改变以下行为：

- 行情、龙虎榜、股票列表、指数、实时行情、账户和历史数据的信息渠道。
- 数据源选择、fallback、缓存、刷新和错误记录逻辑。
- 恒温器策略的市场状态识别、个股状态识别、策略路由、持仓建议、新买候选、网格建议、趋势建议和手工执行计划计算结果。
- 龙虎榜候选池生成、Top N 选择、去重、过滤和排序规则。
- 回测诊断的计算逻辑、统计口径和结果数据。
- 账户初始化、买入、卖出、成本调整、估值刷新、持仓和交易流水逻辑。
- 股票池解析、去重、过滤、剔除科创板和错误提示语义。
- CLI 参数、内部字段名、测试数据结构和持久化数据格式。

汉化只改变用户可见展示，不改变内部逻辑、接口含义、数据结构和计算结果。

## 3. 验收检查范围

本次验收只覆盖正常 Web 使用路径中的用户可见内容，至少包括：

- 恒温器策略页面的表单、运行结果、股票池摘要、市场概览、持仓建议、新买候选、网格建议、趋势建议、手工执行计划和错误区。
- 龙虎榜来源下的时间范围、Top N 选择、候选结果、进度提示和错误提示。
- 回测诊断页面的表单、摘要、诊断明细、交易记录、收益、回撤和错误区。
- 账户页面的账户概览、当前持仓、交易流水、自选组合、账户设置、买入、卖出、成本调整和估值刷新结果。
- 运行中的进度条标题、阶段说明小字和失败提示。

不属于正常 Web 使用路径的内部测试数据、开发者日志、源代码字段名和 CLI 原始输出不作为本次汉化验收对象。

## 4. 不做的范围

本次不要求：

- 重写恒温器策略、回测诊断、账户模块或旧 CLI。
- 更换或新增数据源。
- 改变任何选股、排序、过滤、评分、资金分配或执行辅助算法。
- 改变导出的原始数据字段名，除非该导出明确是面向用户阅读的中文报表。
- 翻译股票代码、证券简称、数据源品牌名、文件路径、URL、异常堆栈、包名、模块名或技术配置值。
- 把开发者调试日志全部改成中文；仅要求正常 Web 页面和用户可见错误提示中文化。

## 5. 汉化范围

正常 Web 页面中，以下内容必须优先汉化：

- 顶部导航、页面标题和页面说明。
- 表单标签、按钮文字、checkbox 文案、下拉选项和输入提示。
- 运行结果区块标题。
- 表格列名。
- 状态值和枚举值。
- 操作建议和策略动作。
- 空状态文字。
- 错误和警告提示。
- 进度条阶段标题和阶段说明小字。
- 结果摘要中的字段名。

如果某个值来自外部数据源且本身是英文，例如供应商返回的英文证券名，可以保留原样；但字段标签必须是中文。

## 6. 结果区标题汉化要求

运行结果中的英文区块标题应显示为中文。至少覆盖以下标题：

- `Stock Pool Summary` 显示为“股票池摘要”。
- `Market Overview` 显示为“市场概览”。
- `Holding Advice` 显示为“持仓建议”。
- `New Buy Candidates` 显示为“新买候选”。
- `Grid Advice` 显示为“网格建议”。
- `Trend Advice` 显示为“趋势建议”。
- `Execution Plan` 显示为“手工执行计划”。
- `Errors` 显示为“错误”。
- `LHB Top 20`、`LHB Top 30`、`LHB Top 50` 显示为“龙虎榜前 20 名”“龙虎榜前 30 名”“龙虎榜前 50 名”。
- `Watchlists` 显示为“自选组合”。
- `Summary` 显示为“摘要”。
- `Diagnostics` 显示为“诊断明细”。
- `Regime Performance` 显示为“市场状态表现”。
- `Trades` 显示为“交易记录”。
- `Positions` 显示为“当前持仓”。
- `Final Pool` 显示为“最终股票池”。
- `Candidate Evaluation` 显示为“候选评估”。
- `New Buy Signals` 显示为“新买信号”。
- `LHB Ranking` 显示为“龙虎榜排名”。
- `Backtest Pool` 显示为“回测股票池”。
- `Backtest Candidate Difference` 显示为“回测候选差异”。
- `Yearly Returns` 显示为“年度收益”。
- `Monthly Returns` 显示为“月度收益”。
- `Monthly Return Matrix` 显示为“月度收益矩阵”。
- `Trade Quality` 显示为“交易质量”。
- `Holding Distribution` 显示为“持仓分布”。
- `Drawdowns` 显示为“回撤”。
- `Symbol PnL` 显示为“个股盈亏”。
- `Equity` 显示为“权益曲线”。

如果新增结果区块，也应遵循中文标题优先原则。

## 7. 表格列名汉化要求

用户可见表格不得直接显示内部字段名。至少覆盖以下列名：

- `symbol` 显示为“证券代码”。
- `code` 显示为“代码”。
- `name` 显示为“名称”。
- `date` 显示为“日期”。
- `start_date` 显示为“开始日期”。
- `end_date` 显示为“结束日期”。
- `watchlist_name` 显示为“自选组合名称”。
- `stock_pool_source` 显示为“股票池来源”。
- `source` 显示为“来源”。
- `source_detail` 显示为“来源说明”。
- `time_range` 显示为“时间范围”。
- `raw_count` 显示为“原始数量”。
- `deduped_count` 显示为“去重后数量”。
- `filtered_count` 显示为“过滤后数量”。
- `excluded_count` 显示为“被剔除数量”。
- `warning` 或 `warnings` 显示为“警告”。
- `error` 或 `errors` 显示为“错误”。
- `market_regime` 显示为“市场状态”。
- `stock_regime` 显示为“个股状态”。
- `confidence` 显示为“置信度”。
- `evidence` 显示为“判断依据”。
- `regime_date` 显示为“状态日期”。
- `data_source` 显示为“数据来源”。
- `data_sufficient` 显示为“数据是否充足”。
- `strategy` 显示为“策略”。
- `strategy_family` 显示为“策略类型”。
- `action` 显示为“建议动作”。
- `strength` 显示为“强度”。
- `score` 显示为“评分”。
- `priority` 显示为“优先级”。
- `suggested_position_pct` 显示为“建议仓位比例”。
- `suggested_shares` 显示为“建议股数”。
- `entry_price` 显示为“入场价”。
- `stop_price` 显示为“止损价”。
- `target_price` 显示为“目标价”。
- `reference_price` 显示为“参考价”。
- `reason` 显示为“原因”。
- `risk_note` 显示为“风险提示”。
- `executable` 显示为“是否可执行”。
- `current_price` 显示为“当前价”。
- `estimated_cost` 显示为“预计成本”。
- `cash_after` 显示为“执行后现金”。
- `recommended_action` 显示为“推荐操作”。
- `fallback_action` 显示为“备选操作”。
- `limit_status` 显示为“涨跌停状态”。
- `volume_limit_pct` 显示为“成交量限制比例”。
- `skip_insufficient_cash` 显示为“资金不足跳过”。
- `skip_volume_limit` 显示为“成交量限制跳过”。

未列出的新增列名只要出现在正常 Web 页面，也必须提供中文展示名。确实无法判断业务含义时，应显示“未翻译字段：原字段名”或等价中文提示，使漏翻问题可见、可定位、可修复。

## 8. 状态值和枚举值汉化要求

用户可见的内部枚举值应显示为中文。至少覆盖以下值：

- `uptrend` 显示为“上升趋势”。
- `downtrend` 显示为“下降趋势”。
- `range` 显示为“震荡区间”。
- `transition` 显示为“转换期”。
- `trend_following` 显示为“趋势跟随”。
- `grid` 显示为“网格策略”。
- `risk_control` 显示为“风险控制”。
- `buy` 显示为“买入”。
- `add` 显示为“加仓”。
- `sell` 显示为“卖出”。
- `hold` 显示为“持有”。
- `observe` 显示为“观察”。
- `wait_confirm` 显示为“等待确认”。
- `blocked` 显示为“暂不参与”。
- `stop_grid` 显示为“停止网格”。
- `high` 显示为“高”。
- `medium` 显示为“中”。
- `low` 显示为“低”。
- `yes`、`true`、`True` 显示为“是”。
- `no`、`false`、`False` 显示为“否”。
- `index_history` 显示为“指数历史数据”。
- `manual` 显示为“手动输入”。
- `watchlist` 显示为“自选组合”。
- `market_range` 显示为“市场范围”。
- `lhb` 显示为“龙虎榜”。

状态值汉化不得改变内部保存和计算使用的原始值。

## 9. 进度提示汉化要求

运行恒温器策略、龙虎榜候选和手工执行计划时，进度展示应使用中文描述。

要求：

- 进度条标题应说明当前总体阶段，例如“正在获取龙虎榜候选”“正在加载候选股历史”“正在评估恒温器”“正在生成手工执行计划”。
- 进度条下方小字应说明当前节点，例如“正在处理 600519.SH，已完成 12 / 50”。
- 如果当前阶段有总数，应显示已处理数量和总数量。
- 如果当前阶段没有总数，应显示具体正在执行的动作，不应只显示英文 stage 名。
- 失败时应显示中文错误摘要，并保留必要的原始错误详情以便排查。

## 10. 错误、警告和空状态汉化要求

正常页面中的错误、警告和空状态应使用中文表达，并告诉用户下一步可以做什么。

至少覆盖：

- 股票池为空。
- 自选组合不存在或为空。
- 龙虎榜候选为空。
- 龙虎榜抓取失败。
- 行情抓取失败。
- 历史数据不足。
- 账户未初始化。
- 当前无持仓。
- 当前无交易流水。
- 无可执行买入候选。
- 手工执行计划因资金不足、涨停、成交量限制或报价缺失而无法生成。

错误详情可以保留外部库返回的原文，但用户可见摘要必须是中文。

## 11. 保留英文的合理情况

以下内容可以保留英文或原始格式：

- 股票代码，例如 `600519.SH`。
- 数据源、库、协议或品牌名，例如 `akshare`、`baostock`、`Sina`、`JoinQuant`。
- 文件路径、账户路径、URL、命令行参数和配置键。
- 外部服务原始异常文本。
- 开发者调试日志。
- 测试断言中验证内部字段的英文键名。

保留英文时，应避免让用户误以为这是需要理解的业务字段；必要时应在旁边提供中文标签或中文说明。外部异常原文如果展示给用户，必须位于中文摘要之后，不能只显示英文异常。

## 12. 验收标准

满足以下条件视为本规格达成：

1. 运行恒温器策略后，结果区标题不再显示 `Stock Pool Summary`、`Market Overview`、`Holding Advice`、`New Buy Candidates`、`Grid Advice`、`Trend Advice`、`Execution Plan`、`Errors` 等英文标题。
2. 运行结果表格不再直接显示 `watchlist_name`、`time_range`、`source_detail`、`market_regime`、`confidence`、`data_source`、`data_sufficient` 等英文列名。
3. 市场状态、个股状态、策略类型和建议动作在页面中显示为中文，例如“震荡区间”“趋势跟随”“观察”“买入”“等待确认”。
4. 龙虎榜结果区的 Top N 标题显示为中文，例如“龙虎榜前 20 名”。
5. 回测诊断、账户页、持仓、交易流水、自选组合等正常页面不再显示本规格第 6 节列出的英文区块标题。
6. 进度条和进度小字使用中文阶段描述，并在可用时显示当前处理股票和已完成数量。
7. 错误、警告和空状态至少有中文摘要，且能告诉用户下一步如何处理。
8. 股票代码、数据源品牌名、文件路径、URL 和外部异常原文可以保留英文；外部异常原文展示时必须带中文摘要。
9. 汉化前后，同一输入下恒温器策略、龙虎榜候选、手工执行计划、回测诊断和账户计算结果保持一致。
10. 汉化不改变信息渠道、内部逻辑、接口语义、持久化数据结构或 CLI 兼容性。
11. 新增或调整的测试能验证关键页面标题、列名和枚举值已中文化。
12. 完整测试通过后，才能认为本规格完成。
