# 自选组合输入规范化与校验规格

## 1. 背景与问题

当前账户页的“自选组合”功能存在输入处理不一致的问题：同样是向自选组合添加股票，有的输入会被自动规范化为带市场后缀的格式，例如 `603309.SH`；有的输入会以用户原始输入的字符串直接保存，例如 `516650,515880,515070,159801`。

这会造成两个用户可见问题：

- 自选组合表格中同时出现规范化代码和未经拆分、未经校验的原始字符串，用户无法判断哪些代码真实可用。
- 使用该自选组合运行恒温器策略时，未经校验的字符串会进入股票池，后续行情查询报错，例如“股票代码应为9位，请检查。格式示例：sh.600000。”，并产生无效候选或“数据不足”结果。

本规格目标是让自选组合的所有股票输入入口遵循一致的拆分、校验、规范化、去重和错误提示规则，避免无效原始字符串静默进入自选组合或策略运行。

## 2. 用户问题表达

用户实际要解决的问题不是单纯的显示格式问题，而是自选组合保存规则不一致：

- 用户希望批量输入股票代码时，系统能自动拆分并检查，而不是把整段输入当成一个股票。
- 用户希望合法股票代码保存为系统统一格式。
- 用户希望不合法或当前系统不支持的代码被明确提示，而不是静默保存。
- 用户希望后续用自选组合运行策略时，不再因为自选组合中混入无效原始字符串而产生行情查询错误。

## 3. 范围

本次规格覆盖：

- 账户页“自选组合”里的创建、添加股票、删除股票、重命名、删除组合相关用户流程。
- 从手动输入股票池保存为自选组合的流程。
- 恒温器策略读取自选组合生成股票池的流程。
- 已存在的自选组合数据中包含异常代码或原始组合字符串时的用户可见处理。
- 自选组合表格、操作结果、错误和警告的展示要求。

## 4. 功能要求

### 4.1 输入拆分

所有向自选组合添加股票的入口都必须支持批量输入。用户可以使用以下分隔方式输入多个代码：

- 英文逗号
- 中文逗号
- 空格
- 换行
- 分号

系统不得把包含多个代码的整段文本作为一个股票代码保存。

### 4.2 代码校验

保存前必须检查每个拆分后的代码是否是当前系统支持的证券代码。

要求：

- 空输入不得保存。
- 非数字、长度不符合要求、带明显错误字符的输入必须提示为无效。
- 当前系统不支持的代码类型必须提示“不支持”或“无法识别”，不得原样保存。
- 本阶段不扩大证券类型支持范围；如果 ETF、基金、可转债等代码当前不被系统支持，应提示用户该代码暂不支持，而不是静默保存。

### 4.3 代码规范化

通过校验的代码必须保存为系统统一格式。

要求：

- 沪市、深市、北交所等当前系统支持的代码应补全为统一市场后缀格式。
- 用户输入是否带后缀不应影响最终保存结果。
- 同一个股票不能因为输入格式不同而在同一组合中重复出现。

### 4.4 去重

同一批输入内的重复代码、以及输入代码与组合内已有代码重复时，都不得重复保存。

操作结果应让用户知道：

- 本次新增了多少个有效代码。
- 有多少个代码因为重复被跳过。
- 有多少个代码因为无效或不支持被拒绝。

### 4.5 错误与警告

系统不得静默失败。出现以下情况时必须在页面上给出可见反馈：

- 输入为空。
- 自选组合不存在。
- 输入中包含无效代码。
- 输入中包含当前系统不支持的代码。
- 输入全部无效，导致没有任何股票被新增。
- 已存在的自选组合包含历史遗留的无效字符串。

错误或警告应尽量包含原始输入片段，便于用户定位问题。

### 4.6 历史异常数据

如果已有自选组合中已经保存了类似 `516650,515880,515070,159801` 这样的异常字符串，系统不能继续把它当作一个有效股票交给策略运行。

要求：

- 自选组合展示时应能让用户看到该组合存在异常代码。
- 使用异常自选组合运行策略时，无效条目必须被排除或阻止进入行情查询。
- 页面应提示用户需要清理或重新添加这些代码。
- 清理前后，自选组合不应影响账户现金、持仓或交易流水。

## 5. 保持不变

本次规格不得改变以下行为：

- 不改变恒温器策略的市场状态判断、个股状态判断、网格建议、趋势建议、候选排序和资金分配逻辑。
- 不改变行情、历史数据、龙虎榜、股票列表等信息渠道。
- 不改变账户现金、持仓、交易流水、买入、卖出、成本调整和估值逻辑。
- 不改变自选组合“不是持仓”的业务含义。
- 不改变现有数据结构的兼容性；已有自选组合数据必须仍可读取。
- 不新增 ETF、基金、可转债等证券类型支持；如需支持，应另开规格。

## 6. 不做范围

本次不要求：

- 重写账户页整体布局。
- 重写恒温器策略或选股引擎。
- 更换或新增行情数据源。
- 自动根据证券代码补全股票名称。
- 自动把自选组合转换成持仓。
- 自动买入、卖出或修改账户资产。
- 批量修复所有历史数据文件而不提示用户。

## 7. 验收标准

满足以下条件才视为本规格完成：

1. 在账户页自选组合中输入 `603309` 后，保存结果显示为系统统一的规范化代码，且不会保存原始 `603309`。
2. 在账户页自选组合中输入 `600519, 000001 300750` 后，系统按多个代码处理，而不是保存为一个整体字符串。
3. 在账户页自选组合中使用换行输入多个代码时，系统按多个代码处理。
4. 对同一组合重复添加同一股票时，最终组合中只保留一条，页面提示重复项被跳过。
5. 输入 `abc`、空字符串或明显不符合股票代码格式的内容时，不会写入自选组合，页面显示错误或警告。
6. 输入当前系统不支持的代码类型时，不会原样写入自选组合，页面显示“不支持”或“无法识别”的提示。
7. 输入 `516650,515880,515070,159801` 时，系统不得把整段文本保存为一个股票代码。
8. 如果其中任何代码当前系统不支持，应逐项提示，而不是在运行策略时才由行情接口报错。
9. 自选组合表格中不得再出现包含多个代码的原始组合字符串作为单个股票。
10. 使用包含异常历史数据的自选组合运行恒温器策略时，异常条目不得进入行情查询；页面应显示可理解的警告或错误。
11. 同一输入在“从手动股票池保存为自选组合”和“账户页添加股票”两个入口下，应得到一致的拆分、校验、规范化和去重结果。
12. 自选组合相关操作不改变账户现金、持仓、交易流水和策略计算逻辑。
13. 相关测试或验证必须覆盖批量输入、重复输入、无效输入、不支持代码、历史异常数据和策略读取自选组合的场景。

## 8. Review/Brainstorming 审查结论

- 用户问题已准确表达：核心是自选组合输入入口的拆分、校验和规范化不一致，导致无效字符串进入策略。
- 验收标准已具体化：每条标准都可以通过页面操作、数据文件检查或自动测试验证。
- 未混入过早实现细节：规格只描述用户可见行为、数据一致性要求和边界，不限定具体函数、组件或文件改法。
- 不做范围已明确：不改策略逻辑、不改数据源、不新增证券类型支持、不影响账户资产。
- 已消除主要含糊点：对当前系统不支持的代码，要求明确提示并拒绝原样保存；如需支持 ETF、基金、可转债，需另开规格。
