# 工作台 UI 重构规格

## 1. 背景和目标

当前本地 Web 已经以“恒温器策略 / 回测诊断 / 账户”为主入口，但页面仍像功能表单集合，而不是可长期使用的工作台：

- 恒温器策略页面一次性展示太多字段，用户需要在同一屏判断哪些字段有效。
- 股票池功能同时出现在恒温器策略页和账户页，保存、管理、选择职责混在一起。
- 账户页所有功能纵向堆叠，页面过长，常用信息和低频操作没有分层。
- 一些字段应该来自账户、股票池或日期范围状态，却要求用户重复手动填写。
- 表单视觉层级弱，缺少清晰分组、折叠、弹窗、抽屉、tab 或二级入口。

本次目标是把本地 Web 调整为更清晰的“工作台”体验：顶部导航保持稳定，页面内部按任务分区，常用信息默认展示，低频操作收起或放入二级入口，字段根据用户选择动态显示。

本规格只描述 UI 行为、信息架构、交互规则、约束和验收标准，不规定具体代码结构、前端框架、CSS 实现、函数签名或文件拆分。

## 2. 顶层导航要求

顶部导航必须继续保留三个主入口：

- 恒温器策略。
- 回测诊断。
- 账户。

顶部导航不应重新暴露旧策略列表、旧默认技术筛选或旧海龟系统入口。旧 CLI、旧筛选引擎和旧海龟源码可以保留为兼容代码，但正常 Web 使用路径不可见。

## 3. 保持不变的范围

本次 UI 重构不得改变以下行为：

- 恒温器策略的市场状态识别、个股状态识别、策略路由、持仓建议、新买候选、网格建议和趋势建议。
- 回测诊断的计算语义和输出数据。
- 账户初始化、买入、卖出、成本调整、持仓估值、交易流水和盈亏统计逻辑。
- 行情抓取渠道、数据源选择、fallback、缓存和错误记录行为。
- 执行辅助中的资金、手数、涨跌停、报价可用性和流动性检查。
- 股票池解析、去重、过滤、错误提示和来源追溯语义。
- 本地 Web 启动方式。

页面优化只改变信息展示、字段组织和交互路径，不得改变策略和账户计算结果。

## 4. 不做的范围

本次不要求：

- 重写恒温器策略。
- 重写海龟系统或改变海龟系统计算结果。
- 重写行情 provider。
- 重写账户交易模型。
- 自动下单。
- 引入新的投资判断逻辑。
- 证明某种 UI 选择能提升策略收益。
- 删除旧 CLI、旧筛选引擎或旧海龟源码。

## 5. 全局工作台布局要求

每个主页面内部必须有清晰分区：

- 页面标题和简短状态说明。
- 常用信息默认展示。
- 操作入口按任务分组。
- 低频或高级操作放入弹窗、抽屉、折叠面板、tab 或二级页面。
- 长表单不得默认完整铺开。

页面最大宽度应合理，内容不应贴边。表单应使用统一的 grid 布局，同一组字段宽度保持一致。按钮位置应稳定，主操作和次要操作应有明显区分。

窄屏或移动端下，页面仍应可用：卡片和表单字段可以换行，但不应出现输入项挤压、文字重叠或操作按钮难以找到的情况。

## 6. 恒温器策略页面目标

恒温器策略页面应聚焦“选择股票池、选择策略日期、查看账户可用状态、运行策略”。

默认页面应展示：

- 股票池来源选择。
- 当前股票池摘要或空状态。
- 策略日期范围选择。
- 当前账户现金或账户未初始化提示。
- 主要运行按钮。
- 低频设置入口，例如数据源、刷新、执行计划、模拟资金等。

不应默认展示所有可能字段。字段必须根据用户选择动态显示。

## 7. 恒温器策略：股票池来源交互

### 7.1 手动输入

当股票池来源选择“手动输入”时：

- 主页面不应直接展示普通股票代码输入框。
- 主页面应展示“编辑手动股票池”按钮或等价入口。
- 点击后打开弹窗、抽屉或独立编辑区。
- 编辑区中支持使用逗号、空格、换行分隔股票代码。
- 编辑区应显示已识别股票数量。
- 编辑区应提示无效代码和重复代码。
- 用户可以选择“仅本次使用”。
- 如果用户希望保存为自选组合，应跳转到账户页的自选组合管理，或调用账户页统一的保存逻辑。
- 恒温器策略页不应单独重复一套“保存手动股票池”区域。

### 7.2 自选股组合

当股票池来源选择“自选股组合”时：

- 用户不应手动填写组合名称。
- 系统应从账户中已有自选组合读取列表。
- 组合选择应展示为下拉框、列表或卡片选择。
- 每个组合至少应展示名称和股票数量。
- 如果没有自选组合，应显示空状态：“暂无自选组合，请到账户页创建”。
- 自选组合的创建、删除、重命名、添加股票、删除股票应统一在账户页完成。

### 7.3 市场范围

当股票池来源选择“市场范围”时：

- 市场范围应支持多选，不应只支持单选。
- 至少支持沪深 A 股、沪市、深市、创业板、科创板、北交所。
- 用户可以同时选择多个市场范围。
- 页面应显示所选范围的摘要。
- 如果选择范围过大，应保留“大范围股票池可能耗时较长”的提示。
- 如果市场范围为空，应显示明确空状态或错误提示。

### 7.4 龙虎榜

当股票池来源选择“龙虎榜”或“同花顺龙虎榜”时：

- 应显示龙虎榜时间范围选择。
- 默认时间范围选项为：最近 1 周、最近 1 个月、最近 3 个月、最近半年、最近 1 年、自定义。
- 不应默认同时显示开始日期和结束日期。
- 只有选择“自定义”时，才显示开始日期和结束日期。
- 结果或摘要必须显示真实数据来源、时间范围、股票数量、错误或警告。
- 如果同花顺龙虎榜不可用，必须明确说明不可用原因和实际使用的数据来源，不得把其他来源伪装成同花顺。

## 8. 恒温器策略：策略日期范围

恒温器策略页面应提供“策略日期范围”选择。

默认选项为：

- 最近 1 个月。
- 最近 3 个月。
- 最近半年。
- 最近 1 年。
- 自定义。

只有用户选择“自定义”时，才显示开始日期和结束日期输入框。

如果用户选择固定范围，系统应自动计算开始日期和结束日期。页面应让用户看见实际使用的日期范围，但不要求用户手动填写。

## 9. 恒温器策略：可用现金

可用现金不应在策略页面默认作为手动输入字段。

要求：

- 系统应从当前账户状态读取可用现金。
- 恒温器策略页面将账户现金显示为只读信息。
- 如果账户未初始化，显示提示：“账户未初始化，请先到账户页初始化账户”。
- 可以保留“使用模拟资金”选项。
- 模拟资金必须明确标注为临时策略测算，不会改变账户现金、持仓或交易流水。
- 只有启用“使用模拟资金”时，才显示模拟资金输入框。

## 10. 恒温器策略：低频设置

以下字段不应默认全部展开：

- 历史源。
- 股票列表源。
- 实时源。
- 强制刷新。
- 生成手工执行计划。
- 次日溢价上限。
- 成交量限制。

这些字段应放入“高级设置”“数据与执行设置”或类似折叠区。默认状态下，用户只应看到当前设置摘要或一个可展开入口。

## 11. 账户页面目标

账户页应重构为“账户概览 + 功能操作区”的工作台结构。

页面顶部展示账户概览。页面下方通过 tab、accordion、sidebar、弹窗、抽屉或二级页面承载具体操作。账户页不应把所有功能表单纵向展开在同一长页面中。

推荐功能操作区分组为：

- 自选组合。
- 账户设置。
- 持仓与估值。
- 买入 / 卖出。
- 成本调整。
- 交易记录。

同一时间默认只展开当前功能分组，其他分组保持收起或通过入口进入。

## 12. 账户概览

账户页顶部应使用卡片式布局展示账户概览。

至少展示：

- 本金。
- 现金。
- 持仓市值。
- 总资产。
- 总收益。
- 总收益率。
- 已实现盈亏。
- 浮动盈亏。
- 持仓数量。
- 胜率。
- 盈亏比。
- 最大回撤。
- 佣金率。
- 印花税率。

如果账户未初始化，应展示明确空状态和初始化入口。未初始化状态下，不应显示误导性的零收益卡片。

## 13. 当前持仓和交易流水

账户页默认应展示当前持仓和交易流水摘要，而不是完整长表。

当前持仓要求：

- 如果有持仓，展示摘要表或卡片。
- 如果无持仓，显示空状态：“暂无持仓”。
- 提供“查看全部”入口。

交易流水要求：

- 默认只显示最近 5 条。
- 如果无交易，显示空状态：“暂无交易流水”。
- 提供“查看全部”入口，进入二级页面、弹窗或展开完整表格。

## 14. 账户功能：自选组合管理

自选组合管理统一放在账户页。

应支持：

- 创建组合。
- 删除组合。
- 重命名组合。
- 添加股票。
- 删除股票。
- 查看组合内股票。
- 从手动输入股票池保存为组合。

输入规则：

- 股票代码支持逗号、空格、换行分隔。
- 添加后显示解析结果。
- 重复代码应提示或显示去重结果。
- 错误代码必须提示，不得静默失败。
- 删除自选组合或组合内股票不得影响账户持仓、现金、交易流水或历史行情缓存。

## 15. 账户功能：账户设置和初始化

初始化账户应位于“账户设置”或“初始化账户”入口中。

字段包括：

- 本金。
- 佣金率。
- 最低佣金。
- 印花税率。

初始化后，账户概览应更新。初始化操作应有明确确认或结果提示，避免用户误以为只是临时测算。

## 16. 账户功能：持仓与估值

刷新行情 / 更新估值应放在“持仓与估值”分组中。

字段包括：

- 账户路径。
- 标记价格。
- 历史源。
- 股票列表源。
- 实时源。
- 是否强制刷新。

点击刷新后，账户概览、当前持仓、持仓市值和浮动盈亏应同步更新。

## 17. 账户功能：手动买入 / 手动卖出

买入和卖出应放在同一分组中，可以使用子 tab、左右分栏或折叠面板区分。

常用字段应默认展示，例如：

- 股票代码。
- 成交价。
- 股数。
- 目标卖出价，若适用于买入。

以下字段应放入“高级信息”折叠区，不应默认全部展开：

- 策略。
- 系统。
- 原因。
- 信号日。
- 执行日。
- 备注。

买入和卖出表单布局应整齐，按钮应固定在表单底部或右下方。

## 18. 账户功能：调整成本

调整成本属于低频操作，应放在单独入口、折叠面板或低频 tab 中。

默认账户页不应让调整成本表单占据大面积页面。进入该功能后，用户应能清楚看到这是会修改持仓成本记录的操作。

## 19. 视觉和交互要求

页面应满足以下视觉要求：

- 页面最大宽度合理，内容与窗口边缘保持留白。
- 表单字段使用统一 grid 布局。
- 同一组表单字段宽度一致。
- checkbox 与文字水平对齐，不应孤立占位。
- 一级标题、二级标题、说明文字、输入框之间层级清晰。
- 主操作按钮和次要按钮视觉区分明确。
- 空状态设计明确，例如“暂无自选组合”“暂无持仓”“暂无交易流水”。
- 长表单使用折叠、弹窗、抽屉、tab 或二级页面，不无限向下堆叠。
- 窄屏下表单和卡片可换行，按钮仍可见。

## 20. 页面职责边界

恒温器策略页负责：

- 选择股票池来源。
- 临时编辑手动股票池。
- 选择策略日期范围。
- 展示账户现金状态或模拟资金状态。
- 运行恒温器策略。
- 展示策略运行结果和股票池摘要。

账户页负责：

- 管理自选组合。
- 初始化账户。
- 刷新估值。
- 记录买入和卖出。
- 调整成本。
- 查看持仓和交易流水。
- 展示账户概览。

恒温器策略页不应重复承担自选组合完整管理功能。账户页不应承载恒温器策略运行表单。

## 21. 错误和空状态要求

系统应明确处理以下状态：

- 账户未初始化。
- 无自选组合。
- 自选组合为空。
- 手动股票池为空。
- 手动股票池包含无效代码。
- 市场范围未选择。
- 市场范围结果为空。
- 龙虎榜结果为空。
- 龙虎榜抓取失败。
- 同花顺龙虎榜不可用。
- 交易流水为空。
- 当前持仓为空。

错误和空状态应告诉用户下一步可以做什么，例如到账户页初始化账户、创建自选组合、取消过滤、切换股票池来源、选择自定义日期或刷新数据。

## 22. 验收标准

满足以下条件视为本规格达成：

1. 顶部导航仍只有恒温器策略、回测诊断、账户三个正常入口。
2. Web 正常路径不显示旧策略列表、旧默认技术筛选或旧海龟系统入口。
3. 恒温器策略页不再一次性铺开所有股票池输入字段。
4. 手动输入股票池通过弹窗、抽屉或等价编辑入口完成，不在主页面直接展示普通输入框。
5. 手动股票池编辑区支持逗号、空格、换行分隔，并显示识别数量、重复代码和错误代码。
6. 恒温器策略页不再单独展示“保存手动股票池”区域。
7. 自选股组合来源从账户已有组合读取，不要求用户手动填写组合名称。
8. 无自选组合时显示“暂无自选组合，请到账户页创建”或等价空状态。
9. 市场范围支持多选，且支持沪深 A 股、沪市、深市、创业板、科创板、北交所。
10. 龙虎榜开始日期和结束日期只在选择自定义时间范围时显示。
11. 龙虎榜时间范围包含最近 1 周、最近 1 个月、最近 3 个月、最近半年、最近 1 年、自定义。
12. 策略日期范围包含最近 1 个月、最近 3 个月、最近半年、最近 1 年、自定义。
13. 策略开始日期和结束日期只在选择自定义策略日期范围时显示。
14. 可用现金从账户读取并只读展示。
15. 账户未初始化时，恒温器策略页提示用户先到账户页初始化账户。
16. 只有启用“使用模拟资金”时，才显示模拟资金输入框。
17. 低频数据源和执行设置默认收起。
18. 账户页顶部以卡片形式展示账户概览。
19. 账户概览至少包含本金、现金、持仓市值、总资产、总收益、总收益率、已实现盈亏、浮动盈亏、持仓数量、胜率、盈亏比、最大回撤、佣金率、印花税率。
20. 账户页默认只展示交易流水最近 5 条，并提供查看全部入口。
21. 账户页默认对无持仓、无交易流水、无自选组合显示明确空状态。
22. 账户功能区使用 tab、折叠、侧栏、弹窗或二级页面组织，不再把所有表单纵向展开。
23. 自选组合管理统一在账户页完成。
24. 手动买入 / 卖出的高级字段默认收起。
25. 调整成本默认不占据大面积页面。
26. 页面排版在窄屏下仍可用，不出现明显重叠或按钮不可见。
27. UI 重构不改变恒温器策略、回测诊断、账户交易、执行辅助、海龟系统计算结果。
