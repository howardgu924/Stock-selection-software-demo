# Web 工作台进度与表单状态统一规格

## 1. 背景

当前 Web 工作台已经包含恒温器策略、回测诊断和账户三个顶部页面。近期使用中暴露出两个明确问题：

1. 恒温器策略只有选择龙虎榜来源时才显示进度条，手动输入、自选股组合、市场范围等来源运行时没有统一进度反馈。
2. 账户页部分表单提交后，输入框里的临时文本会被保留，导致下一次操作时出现无关残留内容。

此外，当前页面状态由全局表单状态复用，不同页面和不同表单之间存在字段名交叉，例如 `symbol`、`watchlist_name`、`path`、`account_path` 等。这会增加状态串扰风险。

本规格目标是统一 Web 端运行流程和表单状态规则，使页面行为更一致、更可预期。此规格只定义用户可见行为和验收标准，不规定具体实现方案。

## 2. 总体目标

1. 恒温器策略的所有股票池来源都使用同一套 job/progress 用户体验。
2. 用户点击“运行恒温器策略”后，无论来源是手动输入、自选股组合、市场范围还是龙虎榜，都应看到统一进度区域。
3. 账户页和恒温器页的输入状态应按页面和表单用途隔离，不应因为一次操作把无关输入长期保留在其他表单里。
4. 表单提交成功后，临时输入字段应清空；需要保留的配置字段应保留。
5. 检查并消除当前 Web 表单、状态、来源选项、进度展示中明显不统一或不合理的行为。

## 3. 范围

### 3.1 本次包含

- 恒温器策略运行入口的进度体验统一。
- 手动输入、自选股组合、市场范围、龙虎榜来源的运行流程一致性。
- 账户页表单提交后的字段清理规则。
- 页面状态保存范围和字段串扰问题。
- 账户路径字段的一致性要求。
- 股票池来源选项和后端支持能力的一致性检查。
- 进度信息展示的完整性和可验证性。
- Web 端相关测试或手动检查标准。

### 3.2 本次不包含

- 不修改恒温器策略计算逻辑。
- 不修改市场判断、个股判断、网格评分、仓位建议等策略规则。
- 不修改账户资金、持仓、交易流水的数据含义。
- 不新增真实交易或自动下单能力。
- 不更换行情数据源。
- 不重写旧 CLI。
- 不重构整个 Web 应用为新框架。
- 不改变现有回测诊断的核心计算结果。

## 4. 恒温器运行进度统一

### 4.1 统一入口

恒温器策略页面中，“运行恒温器策略”按钮应对所有股票池来源提供一致的用户体验：

- 手动输入
- 自选股组合
- 市场范围
- 龙虎榜

无论选择哪一种来源，用户点击运行后都应看到同一类进度区域，而不是只有龙虎榜显示进度条。

### 4.2 进度区域显示要求

进度区域应至少显示：

- 当前阶段名称
- 当前阶段说明
- 总进度百分比或进度条
- 当前正在处理的股票代码，若适用
- 已处理数量 / 总数量，若适用
- 完成后的结果区域
- 失败时的错误说明

进度说明不能只显示一个静态进度条。对于恒温器评估阶段，应能让用户知道系统当前在做什么，例如：

- 正在加载候选股票历史数据
- 正在加载市场基准数据
- 正在判断市场状态
- 正在评估候选股票
- 正在评估持仓
- 正在生成执行计划

### 4.3 完成和失败行为

- 任务完成后，结果应显示在同一页面，不要求用户手动刷新。
- 任务失败时，应在进度区域显示失败原因。
- 失败后页面原有输入区域仍应可继续使用。
- 失败不应导致账户状态、持仓或自选组合被误修改。

### 4.4 长任务体验

对于耗时较长的来源，例如市场范围或龙虎榜：

- 页面应持续显示当前阶段。
- 用户不应误以为页面卡死。
- 进度信息应尽量体现股票处理数量。

对于较短任务，例如少量手动输入或自选股组合：

- 也应使用同一套进度组件。
- 即使任务很快完成，也应显示完成状态或直接显示结果，不应出现体验分裂。

## 5. 表单状态和输入清理

### 5.1 总原则

页面可以保留“配置类字段”，但不应保留“操作类临时输入”。

配置类字段示例：

- 账户路径
- 股票池来源
- 自选组合选择
- 策略日期范围
- 数据源选择
- 是否剔除科创板
- 是否使用模拟资金

操作类临时输入示例：

- 买入/卖出股票代码
- 买入/卖出价格
- 买入/卖出股数
- 成本调整股票代码
- 成本调整价格
- 自选组合创建名称
- 添加股票输入框
- 删除股票输入框
- 重命名后的新组合名称
- 标记价格
- 备注
- 原因
- 信号日
- 执行日

### 5.2 账户页提交后清理规则

以下操作成功后，相关输入框应清空：

- 初始化账户：清空本次初始化表单里的输入值，保留账户路径。
- 刷新行情 / 更新估值：清空标记价格等临时输入，保留账户路径和数据源配置。
- 手动买入：清空股票代码、价格、股数、目标卖出价和高级信息，保留账户路径。
- 手动卖出：清空股票代码、价格、股数和高级信息，保留账户路径。
- 调整成本：清空股票代码、正确平均成本和备注，保留账户路径。
- 创建自选组合：清空组合名称，保留账户路径。
- 添加股票：清空股票代码输入，保留账户路径和当前自选组合选择。
- 删除股票：清空股票代码输入，保留账户路径和当前自选组合选择。
- 重命名组合：清空新组合名称，保留账户路径和当前自选组合选择。
- 删除组合：不应留下已删除组合名称作为当前选中值。

### 5.3 恒温器页提交后清理规则

恒温器页面的运行配置可以保留，便于重复运行：

- 股票池来源
- 自选组合
- 市场范围
- 龙虎榜时间范围
- 策略日期范围
- 账户路径
- 数据源配置
- 剔除科创板选项

手动股票池输入是否保留应遵循以下规则：

- 如果用户选择“仅本次使用”，运行后不应强制写入账户自选组合。
- 页面可以保留本次手动输入，便于用户修改后重跑。
- 如果用户切换到非手动来源，手动输入不应显示在主页面上，也不应影响当前运行。

### 5.4 跨页面状态隔离

不同页面之间不应共享无关表单字段：

- 账户页的 `symbol` 不应带入恒温器页面。
- 恒温器页面的 `symbols` 不应带入账户页买入/卖出输入框。
- 自选组合管理的 `watchlist_name` 不应污染恒温器页当前选择，除非用户明确在恒温器页选择该组合。
- 回测诊断的日期、现金、股票池不应污染恒温器策略页。
- 恒温器页的账户路径和账户页的账户路径应保持含义一致，但不应因为字段名不同导致读取不同账户。

## 6. 账户路径一致性

Web 端应避免让用户同时面对两套含义相同但名称不同的账户路径字段。

用户可见行为应满足：

- 恒温器策略页显示的账户路径与账户页操作使用的账户路径含义一致。
- 用户在账户页切换账户路径后，账户页内所有功能入口都使用同一个路径。
- 用户在恒温器页选择账户路径后，恒温器策略读取同一个账户的现金和持仓。
- 如果内部仍保留不同字段名，用户不应感知到差异。

## 7. 股票池来源一致性

股票池来源下拉中展示的选项，必须与实际可运行能力一致。

要求：

- 如果页面展示“龙虎榜”，后端应能按该来源正常运行。
- 如果页面展示“同花顺龙虎榜”，则应具备对应可用的数据来源说明和运行路径。
- 如果“同花顺龙虎榜”暂不可用，则不应在主选择框中显示为可选项。
- 页面文案不应同时暗示存在两个不同龙虎榜来源，但实际都走同一个来源且没有说明。
- 运行结果中的股票池来源、来源说明、实际日期范围、候选数量应与用户选择一致。

## 8. 错误与提示

错误提示应区分以下情况：

- 任务运行失败。
- 股票池为空。
- 股票代码无效。
- 数据源不可用。
- 账户未初始化。
- 账户路径不存在。
- 现金不足。
- 试探仓金额不足以买入一手。

其中“现金不足”类提示应避免误导：

- 如果账户总现金充足，但策略建议仓位金额不足以买入一手，应明确说“建议仓位金额不足以买入一手”或“试探仓金额不足以买入一手”。
- 不应简单显示“现金不足以买入一手”，让用户误以为账户总现金不足。

## 9. 保持不变

本次优化必须保持：

- 恒温器策略计算结果不因 Web 流程统一而改变。
- 同一输入下，策略输出表的核心字段保持兼容。
- 账户资金、持仓、交易流水不会因为自选组合或恒温器运行被修改。
- 自选组合不是持仓的语义不变。
- 回测诊断页仍可正常运行。
- 页面顶部仍保留：恒温器策略 / 回测诊断 / 账户。
- 现有数据源选择仍可使用。

## 10. 验收标准

1. 手动输入来源点击“运行恒温器策略”后显示进度区域。
2. 自选股组合来源点击“运行恒温器策略”后显示进度区域。
3. 市场范围来源点击“运行恒温器策略”后显示进度区域。
4. 龙虎榜来源点击“运行恒温器策略”后显示同一套进度区域。
5. 四种来源的进度区域结构一致，至少包含阶段、说明、进度、结果或错误。
6. 普通恒温器运行完成后，结果显示在页面上，不需要手动刷新。
7. 普通恒温器运行失败时，错误显示在进度区域或结果区域中。
8. 长任务运行期间，页面能显示当前阶段和已处理数量。
9. 账户页买入成功后，股票代码、价格、股数等输入框清空。
10. 账户页卖出成功后，股票代码、价格、股数等输入框清空。
11. 调整成本成功后，股票代码、成本和备注输入框清空。
12. 创建自选组合成功后，组合名称输入框清空。
13. 添加股票成功后，股票代码输入框清空。
14. 删除股票成功后，股票代码输入框清空。
15. 重命名组合成功后，新组合名称输入框清空。
16. 删除组合成功后，页面不再选中已删除组合。
17. 刷新估值成功后，标记价格输入框清空。
18. 账户路径在账户页各功能中保持一致。
19. 恒温器页读取账户现金时使用的账户路径与页面显示一致。
20. 账户页临时输入不会带入恒温器页。
21. 恒温器页股票池输入不会带入账户页买入/卖出表单。
22. 回测诊断页输入不会污染恒温器页运行配置。
23. 股票池来源下拉不展示不可运行或误导性的来源选项。
24. 如果保留“同花顺龙虎榜”文案，页面必须明确它和普通龙虎榜的区别或当前降级行为。
25. “试探仓金额不足以买入一手”和“账户现金不足”应在提示文案中区分。
26. 所有新增或调整后的用户可见字段不出现“未翻译字段”。
27. 现有恒温器策略计算逻辑不因本次 Web 流程整理发生变化。
28. 现有账户资金、持仓、交易流水语义不变。
29. Web 页面在窄屏下仍可使用，不出现明显按钮或输入框重叠。
30. 工作区不引入临时文件、缓存文件或无关改动。

## 11. 不做范围复核

以下内容不应在本规格下实现：

- 不新增策略参数调优系统。
- 不修改恒温器核心判断阈值。
- 不修改网格或趋势策略规则。
- 不新增自动交易。
- 不重写为前端框架应用。
- 不改变账户文件格式。
- 不改变自选组合文件格式。
- 不把所有页面重新设计一遍。

## 12. 自查结论

- 用户问题已表达为 Web 端运行流程和表单状态统一问题，而不是策略逻辑问题。
- 验收标准均可通过页面行为、HTML 输出、状态字段或现有测试扩展验证。
- 规格中没有限定具体实现方式，例如是否保留当前 job 类、如何拆分表单状态、如何命名内部函数。
- 不做范围已明确，避免把本次工作扩大成策略重写或 Web 全量重构。
- 需要特别注意的潜在冲突是：恒温器手动输入是否保留。规格明确为“可以保留本次输入便于重跑，但切换来源后不得影响当前运行”，避免和“所有输入栏输入后清空内容”发生绝对化冲突。
